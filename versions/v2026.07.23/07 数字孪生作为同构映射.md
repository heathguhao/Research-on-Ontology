# Digital Twin as a Local Isomorphic Mapping: Ontology, Instance Data, and Action

**数字孪生作为局部同构映射：本体、实例数据与动作**

---
作者：顾昊


\medskip

\noindent\textbf{Keywords:} digital twin $\cdot$ local isomorphism $\cdot$ bijection $\cdot$ homomorphism $\cdot$ normalized instance $\cdot$ entity resolution $\cdot$ provenance $\cdot$ governed action $\cdot$ engineering ontology

\medskip

\noindent\textbf{AMS Subject Classification:} 68T30 (Knowledge representation) $\cdot$ 68U35 (Information systems) $\cdot$ 03C05 (Model theory) $\cdot$ 00A71 (Theory of mathematical modeling)

---

## 摘要

数字孪生经常被描述为物理对象的数字表示、同步模型或生命周期信息载体，但这些定义并不要求数字系统成为整个现实世界的完整复制。[1][2][3][^1][^2] 对工程本体论而言，更可检验的说法是：数字孪生由两个内容层构成，即**规范化实例数据**与**抽象本体**；二者之间还需要映射、来源、规则版本和执行契约，才能形成可审计的工程系统。实例数据回答“当前范围内有哪些对象及其状态记录”，抽象本体回答“这些对象可以属于什么类型、具有何种关系、受到哪些规则约束”。

本文把实例层与业务物理世界之间的关系限定为局部实体双射：在业务范围 $S$、对象粒度 $g$ 和时间窗口 $t$ 已经声明的条件下，物理业务实体集合 $P_{S,g,t}$ 与规范化实例集合 $I_{S,g,t}$ 之间建立

\[
\beta_{S,g,t}:P_{S,g,t}\leftrightarrow I_{S,g,t}.
\]

这项双射只承诺实体身份的一一对应，不把属性观测、传感器读数、历史事件或全部业务事实一并宣称为双射。[4][5] 对业务规则，则以受限同态表示：若 $B_S$ 是选定范围内的业务关系与规则结构，$O_S$ 是抽象本体及其可执行规则结构，则

\[
h_S:B_S\rightarrow O_S.
\]

当业务规则 $f_B$ 与数字侧规则 $f_O$ 满足预先声明的结果相容条件

\[
\widehat{\beta}_{S,g,t}\bigl(f_B(x)\bigr)\approx f_O\bigl(\widehat{\beta}_{S,g,t}(x)\bigr),
\]

其中 $\widehat{\beta}$ 是由实体身份双射和状态、关系映射共同诱导的数字表示，不再被宣称为状态值之间的双射。在本文讨论的局部范围和既定颗粒度内，只有实体对应、关系保持、规则相容、来源追踪和现实反馈都得到确认，才把这种数字孪生称为**业务范围内的局部结构同构**。这里的“同构”是一项有限的工程承诺，不是数字世界与全部现实之间的无条件数学等价。[15][16]

基于这一限定，本文给出三类交付物：抽象本体；规范化实例以及实体映射、来源和冲突规则；场景规则与受治理的 Action。其目标不是让系统声称“现实已被完整重建”，而是让每一项数字承诺都能回答：映射了什么、保持了什么、依据是什么、何时失效，以及范围之外如何返回未知。

---

## 1 从严格同构转向局部业务同构

“数字世界是现实世界的同构映射”具有吸引力，因为它把数字孪生从可视化比喻提升为结构承诺。问题在于，现实世界没有天然给出一个边界清晰、粒度固定、时间静止的对象集合。车间里的一台设备可以按整机、部件、传感器或可更换单元建模；同一阀门在安装、停用、拆除和换新后，其身份连续性也可能依据业务规则发生变化。若不先规定范围、粒度和时间，“每个现实对象都有且只有一个数字对象”甚至没有确定的验收对象。

ISO 23247-1 把制造业数字孪生放在明确的观察对象、领域和生命周期语境中；格里夫斯与维克斯、陶飞等人的论述也把物理实体、虚拟表示、数据连接和服务能力区分为相互关联的部分。[1][2][3][^6] 这些来源可以支持“数字表示与物理对象保持联系”，却不能支持“数字表示已经穷尽物理对象的一切性质”。因此，本文不把完整同构当作数字孪生的预设定义，而把**局部结构同构**当作可逐项验收的工程目标。

为此先规定四个对象：

| 记号 | 含义 | 不包含什么 |
|---|---|---|
| $P_{S,g,t}$ | 在范围、粒度和时间条件下纳入管理的物理或业务实体 | 范围外对象、未被声明的粒度、时间窗外的身份状态 |
| $I_{S,g,t}$ | 经实体解析与统一标识形成的规范化实例 | 原始系统中的全部记录、尚未消解的重复项 |
| $B_S$ | 被选中建模的业务关系、约束和状态转换 | 未访谈、未声明或依赖隐性经验的全部规则 |
| $O_S$ | 抽象本体、规则目录及其可解释结构 | 现实本身、外部仿真器的全部内部机理 |

数字孪生的两个内容层可以写成

\[
D_{S,g,t}=\langle I_{S,g,t},O_S\rangle,
\]

但工程交付不能只留下这两个集合。系统还必须保存映射与运行元数据 $M_{S,g,t}$，包括原始标识到规范化标识的对应、来源、有效时间、解析规则版本、规则适用范围、动作契约和组织确认记录。$M_{S,g,t}$ 不是第三个“世界”，而是证明前两个内容层为何可以相互对应的证据层。[10][11][14][^7]

由此，本文所说的数字孪生不是一个全知模型，而是一个带有承诺边界的三元工程对象：

\[
\mathcal D_{S,g,t}=\langle I_{S,g,t},O_S,M_{S,g,t}\rangle.
\]

只有在 $S$、$g$、$t$ 和所选规则签名明确时，“双射”“同态”和“同构”才有可操作的含义。

## 2 数字孪生的两个内容层与一个证据层

### 2.1 规范化实例层：先统一“是哪一个”

客户的监控与数据采集系统（SCADA）、制造执行系统（MES）、企业资源计划系统（ERP）、维修数据库和人工台账可能分别保存同一台设备的记录。这些记录是对业务实体的观察和陈述，不等于业务实体本身。若监控与数据采集系统使用 `V-12`，制造执行系统使用 `VALVE_B3_012`，维修系统使用 `EQ-2021-0457`，第一项任务不是把三个标识分别实例化，而是判断它们是否指向同一个业务对象。

记录链接与实体解析研究长期处理重复记录、近似匹配、候选对比较和匹配决策问题。费莱吉与森特给出了经典的概率记录链接框架，克里斯滕则系统讨论了数据预处理、阻塞、字段比较、分类与评估。[4][5][^8] 这些方法能够提供证据和候选，却不会自动替代现场身份判断。设备序列号、安装位置、拓扑关系、启停时间、维修记录和业务负责人确认，可能共同决定三个源标识是否应合并。

规范化实例层的核心产物是**规范化业务标识**。多个原始标识可以先经解析关系

\[
r:\text{来源记录}\rightarrow I_{S,g,t}
\]

汇聚到一个实例；随后，这个实例才与 $P_{S,g,t}$ 中的一个业务实体参加双射。这里必须区分“记录合并”和“实体双射”：前者允许多条记录指向一个实例，后者要求一个纳入范围的业务实体对应一个规范化实例。[4][5]

规范化实例还应携带状态、关系和观测，但这些内容与实体身份具有不同的时间语义。例如，设备序列号可能相对稳定，温度读数按秒变化，所属班组按排班变化，“正在运行”还可能因采样延迟而不确定。把它们都塞进一个不带时间和来源的“对象快照”，会把身份、观测和判断混为一谈。

### 2.2 抽象本体层：声明系统能够表达什么

抽象本体不是规范化实例的副本，而是对业务类型、关系、属性、约束和部分操作结构的显式规定。格鲁伯强调本体是对概念化的明确规定，并提出清晰性、连贯性和最小本体承诺等设计原则；瓜里诺则区分了概念化、逻辑理论和工程制品等层次。[12][13][^10] 这些原则要求本体说明承诺了什么，也要求它避免把没有证据的现实细节冒充为形式结论。

工程实现必须区分类型定义与实例记录。本体声明系统允许表达哪些对象、关系、属性、约束和动作；实例记录则指向特定业务对象及其状态。新增一台设备并不等于新增一种类型，类型定义存在也不等于现场已经存在对应实例。这个边界既防止把本体写成资产台账，也防止把一条实例记录误当成业务规则。

RDF 与 OWL 的标准进一步说明，序列化结构、模型论解释和运行时数据不是同一层。RDF 定义图与三元组数据模型；OWL 2 的结构规范和直接语义分别规定语法对象与解释条件；SHACL 则验证数据图相对于约束形状图是否符合约束。[6][7][8][9][^13] 因此，“写成三元组”“通过约束形状校验”或“可被 OWL 解释”都不等于业务事实已经真实，也不等于外部 Action 已经安全执行。

### 2.3 映射与来源层：保存为什么这样对应

一个规范化实例至少需要回答：它来自哪些系统记录，使用了哪一版实体解析规则，谁确认了合并或拆分，在哪个时间窗口有效，当前是否存在冲突，以及何时应重新核验。PROV-DM 和 PROV-O 用实体、活动、责任主体及其关系描述来源链；布尼曼等人则从数据库查询角度区分结果“为何产生”与数值“来自何处”。[10][11][14][^14]

来源模型本身不能保证记录完整，但它能够阻止系统把结论伪装成无来源事实。对本文的映射层而言，至少应保存：

- 业务范围、对象粒度和有效时间；
- 原始系统、原始主键和采集时间；
- 规范化实例标识及其类型；
- 合并、拆分、淘汰和替代关系；
- 解析规则版本、置信状态和人工确认记录；
- 属性值的来源、单位、有效时间、质量状态和冲突策略；
- 业务规则的原始表述、本体表示、测试样例和批准版本；
- 动作的调用目标、安全要求、执行结果和外部回执。

这组元数据使双射和同态成为可复核的主张，而不是架构图上的两根箭头。

## 3 局部实体双射：身份一一对应，而非数据全面相等

### 3.1 双射的范围条件

给定 $S$、$g$ 和 $t$，局部实体双射写作

\[
\beta_{S,g,t}:P_{S,g,t}\leftrightarrow I_{S,g,t}.
\]

它要求：

1. 对每个 $p\in P_{S,g,t}$，存在唯一的 $i\in I_{S,g,t}$ 使 $\beta(p)=i$；
2. 对每个 $i\in I_{S,g,t}$，存在唯一的 $p\in P_{S,g,t}$ 使 $\beta^{-1}(i)=p$。

第一项防止一个业务实体被重复实例化，第二项防止数字实例没有可确认的业务对象。二者只对已经进入 $P_{S,g,t}$ 的对象成立。范围外实体不是“漏掉的满射对象”，而是没有进入本次承诺集合；待确认实例也不应提前进入 $I_{S,g,t}$，而应保留在待审查区。[15][16]

这里的“物理业务实体”也不是由数据库凭空给出的集合。企业需要以资产清单、现场盘点、设备铭牌、控制系统拓扑、维修记录和责任人确认等证据建立 $P_{S,g,t}$ 的操作性边界。双射验收因此不是一次纯数学计算，而是数学条件与业务证据的联合审查。

### 3.2 双射不覆盖属性观测和历史记录

同一台阀门可以有多路温度传感器、一条传感器流可以产生无数时间点记录、一个维修事件也可能被多个系统重复记载。把这些记录都要求与物理实体一一对应，会立即破坏双射。因此本文只把**规范化实体身份**放入 $\beta$；属性值、关系断言和事件记录分别使用带时间与来源的陈述结构，例如

\[
o=\langle i,p,v,t_v,t_i,s,q\rangle,
\]

其中 $i$ 是规范化实例，$p$ 是属性，$v$ 是值，$t_v$ 是业务有效时间，$t_i$ 是采集或入库时间，$s$ 是来源，$q$ 是质量状态。[10][11][^16] 同一实例允许有多个观测，也允许在冲突尚未消解时保留多个候选值。

由此需要明确两点。第一，属性不是系统中“唯一携带值的东西”；关系、事件、动作参数、外部返回和来源记录也可以携带值。第二，属性值缺失不意味着实体双射必然失败；它首先意味着该属性在指定时间缺少可信观测，系统应返回未知、降级或待审查，而不是编造一个填充值。[6]

### 3.3 双射是一项持续的时态契约

设备安装、拆除、换件、合并管理单元或重新划分业务粒度，都可能改变 $P_{S,g,t}$。因此 $\beta_{S,g,t}$ 不是永恒不变的映射。它必须记录有效时间，并区分至少四种情况：

- **别名新增**：新源标识被确认仍指向现有实例，不新增业务实体；
- **实体新增**：现场出现新的范围内实体，建立新实例和证据链；
- **实体退役**：实例在新的时间窗口不再属于活动集合，但历史身份仍可追溯；
- **身份争议**：合并或拆分证据不足，映射进入待审查状态，不对外宣称双射成立。

这使“局部双射”成为版本化的治理状态，而不是一次数据清洗后的永久结论。

## 4 业务规则的受限同态

### 4.1 同态保持的是已声明结构

实体对应解决了“是哪一个”，但没有解决“它们之间如何关联”和“规则如何运行”。设 $B_S$ 包含选定业务范围内的关系、约束与操作，$O_S$ 包含本体可表达的对应结构。映射

\[
h_S:B_S\rightarrow O_S
\]

只有在声明的关系或运算被保持时才可称为同态。标准数学意义上的同态总是相对于某种结构签名而言；不说明要保持哪些关系和运算，单独写一根 $h$ 箭头没有证明力。[15][16][^18]

业务规则可以分为四类：

| 规则类型 | 例子 | 数字侧承载方式 | 不能据此推出什么 |
|---|---|---|---|
| 类型与结构约束 | 阀门只能连接规定类型的管段 | 类型系统、约束语言或专用校验器 | 现场连接必然正确 |
| 派生规则 | 根据已确认关系计算责任班组 | 查询、规则引擎或虚拟表达式 | 输入事实没有缺失 |
| 状态转换 | 工单批准后进入可执行状态 | 工作流或状态机 | 物理设备已经动作 |
| 物理效应与因果规则 | 关阀后压力如何变化 | 外部仿真、因果模型或现场反馈 | 仅凭本体推理即可预测现实 |

因此，抽象本体并不是“业务规则的映射本身”，而是业务结构的一种受限表示。$h_S$ 的职责是把选定的业务概念、关系和规则翻译为数字侧可识别的类型、约束、查询、状态机或外部模型接口。不同规则需要不同执行语义，不能统一冒充为 OWL 后承、图遍历或 Action 调用。[6][7][8][17][^19]

### 4.2 交换关系与结果相容

对一个选定业务规则 $f_B$，数字侧实现为 $f_O$。本文用

\[
\widehat{\beta}_{S,g,t}\bigl(f_B(x)\bigr)\approx f_O\bigl(\widehat{\beta}_{S,g,t}(x)\bigr)
\]

表示结果相容。$\widehat{\beta}$ 是 $\beta$ 在已声明状态、关系和输出编码上的提升；它只负责把可比较的业务输入与结果带到数字表示中，不继承实体集合上的双射性质。这里的 $\approx$ 必须由验收协议定义：离散状态可以要求相等；测量值可以允许单位换算、时间偏差和误差带；风险分类可以要求落入同一等级；外部 Action 则可以要求收到设备回执并由后续观测确认。[17]

这条式子不是“数字规则必然复制现实因果”的定理。要使它成为可检验条件，至少需要声明：

1. $x$ 属于哪个对象集合和前置状态；
2. $f_B$ 是业务规范、经验规则、因果模型还是物理过程；
3. $f_O$ 由哪一版规则、代码、模型或服务实现；
4. $\approx$ 使用何种容差、时间窗和测试判据；
5. 输入缺失、规则冲突或外部调用失败时返回什么状态。

若这些条件没有定义，系统最多拥有一条规则类比，不能宣称同态已经成立。还要看到，规则相容只能说明数字世界没有背离企业已经确立的业务逻辑，不能保证现实对象一定产生预期结果；物理反馈则用现场回执和后续观测检验数字表示是否仍与现实相符。两者结合，局部同构才不只是符号系统内部的自洽。

### 4.3 Action 是受治理接口，不是因果一致性的自动保证

动作定义只是把业务规则连接到执行环境的契约位置，不能仅凭一个动作名称，就推定系统已经具备触发判断、前后置条件、失败补偿和回退能力。这些能力必须由具体执行机制和管理要求另行保证。

一项进入生产的动作至少还要说明目标、执行前提、失败处理、物理联锁、返回结果和审计要求。瑞特对行动理论的讨论也表明，动作描述、状态变化和执行结果是需要分别建模的层次。[17] 对“超温关阀”一类示例，温度阈值只能作为说明，不能直接成为真实现场策略；生产执行仍要接受设备状态、安全联锁和外部回执的检验。

动作的作用是把数字侧决策送入受控执行环境，并把执行证据带回实例层。它可以参加规则相容性验收，但不能凭一个形式名称保证物理世界会产生预期结果。

## 5 局部业务同构的验收条件

在标准数学用法中，同构通常要求存在保持结构的双射，并且逆映射也保持相应结构。[15][16][^22] 工程系统面对的是开放、变化且不完全可观测的业务环境，所以本文不把整个现实与整个数字系统宣称为同构。只有当选定签名内的实体、关系和操作同时满足下列条件时，才使用“局部业务结构同构”这一表述：

| 验收维度 | 必须声明或验证的内容 | 失败时的正确状态 |
|---|---|---|
| 范围 | 业务域 $S$、对象粒度 $g$、时间窗口 $t$ | 范围未定义 |
| 身份 | $P_{S,g,t}$ 与 $I_{S,g,t}$ 的覆盖与唯一性 | 重复、缺失或待确认 |
| 类型与关系 | 实例符合声明的类型、基数和关系约束 | 不符合或冲突 |
| 规则保持 | $h_S$ 覆盖哪些关系和运算，哪些未覆盖 | 仅部分同态 |
| 结果相容 | $f_B$、$f_O$ 与 $\approx$ 的测试判据 | 偏差、未通过或不可比较 |
| 时间有效性 | 有效时间、采集延迟、规则版本和实例版本一致 | 过期或时态不一致 |
| 来源与责任 | 数据、映射、规则和 Action 均可追溯 | 来源不足或待审查 |
| 模型外处理 | 缺失、冲突、不确定和外部失败有显式状态 | 未知，不得静默补全 |

SHACL 可以承担一部分结构校验，PROV-O 可以承担一部分来源表达，企业自身的类型系统可以承担一部分定义与动作契约；但没有任何一种规范可以单独完成全部验收。[9][10] 工程上应把结论分成三档：

- **局部结构同构**：身份双射、声明结构保持和结果相容均通过指定测试；
- **同态投影**：数字侧保持一部分业务关系或规则，但存在信息压缩、不可逆关系或模型外过程；
- **实例映射**：仅完成记录到规范化实例的对应，尚未验证规则保持。

这三档不是成熟度口号，而是对系统实际能承诺什么的区分。多数业务数字孪生在大部分时间里会同时包含三种区域：某些局部已通过同构验收，某些规则只有同态投影，另一些对象仍处于实例映射或待确认阶段。

## 6 三类可交付、可验收的工程产物

### 6.1 抽象本体包

第一类交付物规定系统的表达边界，包括对象类型、关系类型、属性类型、接口、约束、规则目录和版本。机器可读定义只是工程骨架，还需要术语定义、业务例子、适用范围、未建模项和验证用例。[12] 质量标准不应写成“完整表达真实业务”，而应写成“在声明的范围与签名内，表达清晰、一致、可验证，并对未覆盖内容显式留空”。

### 6.2 规范化实例与映射来源包

第二类交付物包括规范化实例、跨系统标识对照、实体解析规则、属性来源策略、有效时间、冲突状态、质量状态和责任记录。它回答的不是“哪个数据库最真实”，而是“当前结论综合了哪些证据，为什么把这些记录视为同一对象，以及该判断何时需要重做”。

这类能力可以由集中式数据中台实现，也可以由主数据服务、数据织物、联邦查询或领域数据产品组合实现。**数据治理与映射能力是逻辑必需的，集中式数据中台这一具体架构不是逻辑必然。** 架构选择应由数据量、时效要求和现有系统条件决定，而不是由“双射”一词推出。[4][5][10][14][^24]

### 6.3 规则、场景与受治理 Action 包

第三类交付物把业务规则拆成可执行、可测试和需外部求值的不同部分。它至少包括：

- 规则的业务原文、形式表示、版本和适用范围；
- 正常、边界、冲突、缺失和超时测试；
- 对外部仿真器、因果模型或人工判断的接口；
- 动作的参数边界、安全要求和执行方式；
- 触发、前后置条件、幂等、回退与补偿契约；
- 运行结果、设备回执、后续观测和组织确认记录。

场景分叉和增量影响分析可以成为这类交付物的测试环境：规则先在可追溯场景中求值，涉及生产写入时再接受联锁和管理审查。这让“系统能做什么”与“系统可以安全地在现实中做什么”保持必要距离。

## 7 工程验收的统一边界

数字孪生不能凭借形式化定义证明“现实已经可以被完整复制”。它需要把表达范围、实例身份、规则结构、场景计算和治理责任转化为可检查的验收对象。本文将这些要求归纳为三项：

1. **实例身份是否可靠**：用 $\beta_{S,g,t}$ 检查规范化实例与范围内业务实体的一一对应；
2. **规则结构是否被保持**：用 $h_S$ 和结果相容条件检查选定关系、约束和操作；
3. **系统是否对未知诚实**：对范围外、证据不足、冲突、延迟和执行失败返回明确状态。

因此，数字孪生的可信度来自范围、证据、测试和责任链，而不是来自“同构”这个词本身。有限模型可以承担经过声明和验收的局部承诺，却不能跳跃为对无限现实的完整担保。

## 8 结论：同构不是起点，而是验收结果

数字孪生的核心内容可以简化为：**规范化实例数据 + 抽象本体**。前者承载业务范围内被识别的对象及其状态记录，后者承载这些对象的类型、关系、约束和部分规则。二者之间的映射、来源、规则版本和 Action 契约，使这两个内容层成为可解释、可追溯、可运行的工程系统。

在实例层，本文只对明确范围、粒度和时间窗口中的实体身份提出局部双射，不把所有属性、传感器值和历史记录混入双射。在规则层，本文只对已经声明并能被数字侧表达的业务关系和操作提出受限同态，不把规则运行等同于物理因果必然实现。只有当局部双射、结构保持、结果相容、来源追踪、时间有效性和未知项治理同时通过验收时，才能说数字孪生在该业务范围内形成了局部结构同构。

这项修改没有削弱数字孪生的工程要求，反而使要求变得可执行。完整现实无法被一句“严格同构”担保；局部承诺却可以被盘点、测试、审计和持续修正。数字孪生真正有价值的地方，不是它声称复制了整个现实，而是它能清楚说明：**在什么范围内，哪些数字实例对应哪些业务实体，哪些规则保持了哪些结构，依据何在，以及何时应当回答未知。**

---

## 参考文献

[1] International Organization for Standardization. *ISO 23247-1:2021 Automation Systems and Integration — Digital Twin Framework for Manufacturing — Part 1: Overview and General Principles*. Geneva: ISO, 2021. https://www.iso.org/standard/75066.html

[2] Grieves, M. and Vickers, J. “Digital Twin: Mitigating Unpredictable, Undesirable Emergent Behavior in Complex Systems.” In: Kahlen, F.-J., Flumerfelt, S. and Alves, A. (eds.), *Transdisciplinary Perspectives on Complex Systems*. Cham: Springer, 85–113, 2017. DOI: 10.1007/978-3-319-38756-7_4

[3] Tao, F., Zhang, H., Liu, A. and Nee, A. Y. C. “Digital Twin in Industry: State-of-the-Art.” *IEEE Transactions on Industrial Informatics*, 15(4): 2405–2415, 2019. DOI: 10.1109/TII.2018.2873186

[4] Fellegi, I. P. and Sunter, A. B. “A Theory for Record Linkage.” *Journal of the American Statistical Association*, 64(328): 1183–1210, 1969. DOI: 10.1080/01621459.1969.10501049

[5] Christen, P. *Data Matching: Concepts and Techniques for Record Linkage, Entity Resolution, and Duplicate Detection*. Berlin: Springer, 2012. DOI: 10.1007/978-3-642-31164-2

[6] W3C RDF Working Group. *RDF 1.1 Concepts and Abstract Syntax*. W3C Recommendation, 25 February 2014. https://www.w3.org/TR/rdf11-concepts/

[7] W3C OWL Working Group. *OWL 2 Web Ontology Language: Direct Semantics (Second Edition)*. W3C Recommendation, 11 December 2012. https://www.w3.org/TR/owl2-direct-semantics/

[8] W3C OWL Working Group. *OWL 2 Web Ontology Language: Structural Specification and Functional-Style Syntax (Second Edition)*. W3C Recommendation, 11 December 2012. https://www.w3.org/TR/owl2-syntax/

[9] W3C Data Shapes Working Group. *Shapes Constraint Language (SHACL)*. W3C Recommendation, 20 July 2017. https://www.w3.org/TR/shacl/

[10] W3C Provenance Working Group. *PROV-O: The PROV Ontology*. W3C Recommendation, 30 April 2013. https://www.w3.org/TR/prov-o/

[11] W3C Provenance Working Group. *PROV-DM: The PROV Data Model*. W3C Recommendation, 30 April 2013. https://www.w3.org/TR/prov-dm/

[12] Gruber, T. R. “Toward Principles for the Design of Ontologies Used for Knowledge Sharing.” *International Journal of Human-Computer Studies*, 43(5–6): 907–928, 1995. DOI: 10.1006/ijhc.1995.1081

[13] Guarino, N. “Formal Ontology and Information Systems.” In: Guarino, N. (ed.), *Formal Ontology in Information Systems: Proceedings of FOIS ’98*. Amsterdam: IOS Press, 3–15, 1998.

[14] Buneman, P., Khanna, S. and Tan, W.-C. “Why and Where: A Characterization of Data Provenance.” In: *Database Theory — ICDT 2001*. Lecture Notes in Computer Science, Vol. 1973. Berlin: Springer, 316–330, 2001. DOI: 10.1007/3-540-44503-X_20

[15] Mac Lane, S. *Categories for the Working Mathematician*, 2nd ed. New York: Springer, 1998.

[16] Dummit, D. S. and Foote, R. M. *Abstract Algebra*, 3rd ed. Hoboken, NJ: Wiley, 2004.

[17] Reiter, R. *Knowledge in Action: Logical Foundations for Specifying and Implementing Dynamical Systems*. Cambridge, MA: MIT Press, 2001.

[18] National Institute of Standards and Technology. *Artificial Intelligence Risk Management Framework (AI RMF 1.0)*. NIST AI 100-1, 2023. DOI: 10.6028/NIST.AI.100-1

---

## 脚注

[^1]: ISO 23247-1 的对象是制造业数字孪生框架的概览与一般原则。正文只据此说明数字孪生需要被放在确定的制造域、观察对象和生命周期语境中，不把该标准改写成“数字世界与全部现实严格同构”的定义。[1]

[^2]: Grieves 与 Vickers讨论物理系统、虚拟系统及其连接；Tao 等人综述数字孪生在工业中的模型、数据、连接和服务。两者支持“数字表示与物理对象持续关联”，不支持“数字模型已经穷尽对象的全部性质”。[2][3]

[^6]: ISO 标准、Grieves–Vickers 章节与 Tao 等人的综述使用的模型组成和术语并不完全相同。正文只提取三者共同允许的最低判断：数字孪生包含物理对象、数字表示以及数据或服务联系；不把其中任一来源宣称为唯一权威定义。[1][2][3]

[^7]: $M_{S,g,t}$ 是本文把来源、映射和运行契约合并后的工程记号。PROV-DM/PROV-O 可以表达其中一部分来源关系，但不能自动补齐实体解析规则、业务时间、动作安全契约或组织确认过程。[10][11][14]

[^8]: Fellegi 与 Sunter 1969 研究两份记录属于同一个体的链接决策；Christen 2012 覆盖数据匹配与实体解析的完整工程过程。正文据此支持“统一标识需要证据与决策”，不据此声称算法能够单独确定工业现场真相。[4][5]

[^10]: Gruber 的设计原则是本体工程的规范性建议；Guarino 的区分用于澄清概念化、理论和制品层次。二者都不能单独证明某个业务本体与外部现实一致。[12][13]

[^13]: RDF 1.1 定义图和三元组；OWL 2 Structural Specification 定义语法结构，Direct Semantics 定义解释和后承；SHACL 定义数据图相对 shapes graph 的 conformance。四者职责不同，均不直接验证物理事实或 Action 安全。[6][7][8][9]

[^14]: PROV-O/PROV-DM 提供来源表达词汇和数据模型；Buneman 等人分析查询结果为何出现以及值来自哪里。正文据此要求保存来源和生成活动，但“采用 PROV”不等于来源记录必然完整或真实。[10][11][14]

[^16]: 观测结构 $o=\langle i,p,v,t_v,t_i,s,q\rangle$ 是本文的最小示意，不是 PROV 或 RDF 的标准消息格式。实际实现还需规定单位、时区、精度、空值、冲突、修订和保留策略。[10][11]

[^18]: 同态是否成立取决于所选代数、关系结构或范畴中的运算与态射。正文使用 $h_S$ 时必须同时列出被保持的类型、关系或操作，不能只凭命名断定结构已经保持。[15][16]

[^19]: OWL 后承相对于指定解释定义；RDF 图不自动包含状态转换语义；Reiter 的行动理论也需要动作、状态和结果规则。正文因此把静态约束、派生规则、工作流转换和物理效应分开处理。[6][7][8][17]

[^22]: 本文不把“有一个双射和一个单向同态”自动等同于完整同构。只有在选定结构内还能够验证逆向结构保持或等价的可恢复条件时，才使用局部结构同构；否则应称为同态投影。[15][16]

[^24]: 数字孪生并不要求“数据中台”成为逻辑上必然存在的唯一中间层。真正不可缺少的是实体统一、来源、时间、冲突和映射治理能力；这些能力可以由不同架构实现。[4][5][10][14]

---

作者：顾昊
