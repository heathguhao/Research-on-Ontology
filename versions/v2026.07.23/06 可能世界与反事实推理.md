# Possible Worlds and Counterfactual Reasoning: The "What If" in Engineering Ontology

**可能世界与反事实推理：工程本体论中的“如果”**

---
作者：顾昊


\medskip

\noindent\textbf{Keywords:} possible worlds $\cdot$ counterfactual reasoning $\cdot$ modal logic $\cdot$ scenario analysis $\cdot$ snapshot $\cdot$ dependency analysis $\cdot$ simulation $\cdot$ causal model $\cdot$ abduction

\medskip

\noindent\textbf{AMS Subject Classification:} 03B45 (Modal logic) $\cdot$ 03B70 (Logic in computer science) $\cdot$ 68T30 (Knowledge representation) $\cdot$ 68U20 (Simulation) $\cdot$ 03A05 (Philosophical logic)

---

## 摘要

工程人员经常提出“如果把某个阈值改小”“如果某个传感器失效”“如果两路供电同时中断”这样的问题。可能世界和反事实逻辑为这些问题提供了一种改变前提、观察后果的说理方式，但逻辑上能够推出什么，不等于物理世界一定会发生什么。本文讨论的是这种有限的逻辑分析，不宣称已经解决现实因果识别或物理预测。[1][2][3][^1]

本文把反事实分析理解为一种受约束的逻辑推演：给定基线、明确的假设、选定的规则和具体问题，比较前提变化前后哪些结论随之改变。本文介绍两条分析路径：**场景分叉**，在基线上形成一个只读的假设分支；**增量影响分析**，沿依赖关系重算受影响的结论。二者都不是刘易斯反事实语义的“可判定实例化”，也不是珀尔 `do` 算子的同构实现，只是借鉴相关思想的逻辑组织方式。[1][2][3][4][^2]

版本记录本身不能承担过去、现在和未来时态算子的完整语义，规则闭包也不能自动成为一个世界的全部事实。反事实结果必须说明模型范围、未知项、冲突状态和来源；涉及物理效应时，逻辑分析只能给出条件性的解释，不能冒充仿真或现场结果。本文的目标不是提供一套无所不包的解决方案，而是让“如果”得到清楚、可比较的表达。

---

## 1 引言：工程系统需要什么样的“如果”

形式语义与工程选择必须分开：内涵逻辑可以在模型论层面表达索引、作用域和可能世界，但工程本体只对声明过的类型、数据、规则和任务作有限承诺。本文处理一个实践问题：当系统不能、也不应把所有可能情形都装进一个逻辑时，如何支持故障分析、设计变更、应急推演和历史追溯。

这里首先要纠正一个前提。一阶逻辑的语言签名没有内置的模态算子，并不意味着一阶逻辑“失去了所有世界表达能力”。可以把世界、时间点或场景作为个体，引入可达关系、状态关系和满足关系，再用一阶公式描述它们；模态逻辑到一阶逻辑的标准翻译正是这种做法的形式背景。[5][^4] 代价是：世界结构、可达关系和满足条件需要显式建模，系统不再自动获得某一种内涵语义。工程上真正需要解决的，不是“把可能世界找回来”，而是决定哪些场景值得建模、哪些规则可以运行，以及结果如何被解释和追责。

本文将以下四种工作分开：

| 工作 | 主要问题 | 典型输出 |
|---|---|---|
| 模态或反事实语义 | 某个前件在与基线相关的情形中是否支持后件 | 真值或条件判断 |
| 因果干预 | 改变一个变量的生成机制后，其他变量如何变化 | 因果效应或分布 |
| 仿真与故障注入 | 在受控副本中施加扰动，系统如何响应 | 轨迹、告警和安全指标 |
| 版本化场景分析 | 在某个历史或当前基线应用假设补丁后，哪些规则结果改变 | 差异集、未知项和审计记录 |

第四行是本文的工程对象。它可以调用前三行的模型或工具，但不因使用“可能世界”这个名称就获得前三行的全部语义。

### 1.1 前人的相关工作与本文的定位

刘易斯以与基线世界的相似性排序解释反事实条件句，斯塔尔内克以选择函数选择与基线最相关的前件世界；二者都是关于条件句真值条件的哲学语义。[1][2][^5] 珀尔的结构因果模型则用结构方程、外生变量和干预操作定义因果反事实；`do(X=x)` 不是简单地把数据库中的一列改成 `x`。[3][4][^6] AGM 信念修正理论讨论知识库如何在新信息与旧承诺冲突时进行收缩和修正，提醒我们“增加一个假设”“删除旧事实”和“恢复一致性”是不同操作。[6][^7]

安全关键系统中的故障注入，则在隔离或仿真环境中主动施加故障条件，观察保护机制和依赖系统的反应。[7][17][^8] 数据库和规则系统中的增量视图维护研究了删除、插入和修改如何传播到派生结果；它可以为本文的增量影响分析提供算法背景，但不自动提供因果解释。[8][9][^9]

本文相对于这些工作的位置是有限的：它不提出新的反事实语义或因果识别定理，而是把场景分支、增量计算、版本溯源和治理门组合成工程本体可以承载的一组操作协议。

## 2 四种工程场景

以下例子是设计场景，不是对某个真实工厂的性能或风险统计。每个场景都必须在实施时补充数据来源、模型版本和验收指标。[^10]

### 2.1 故障仿真与故障注入

假设 V-12 的温度传感器存在偏移，系统需要评估保护逻辑是否仍会在实际超温时动作。这个问题不能只把“读数”改成另一个数字：如果真实温度、传感器读数、阈值、控制逻辑和执行延迟之间没有明确关系，规则引擎只能报告已编码关系下的结果，不能自动推断物理真实。

工程实现可以在仿真副本中注入“传感器偏移”故障，观察告警、动作请求和安全裕度；也可以调用已经验证的传感器误差模型。故障注入的结果应区分“保护逻辑被触发”“动作请求生成”“执行成功”和“设备实际状态”，不能把其中一个状态直接当成另外三个状态。[7][17][^8]

### 2.2 设计变更的连锁效应

运维团队可能提出“将 V-12 的触发阈值从 300°C 改为 250°C”。如果系统只保存阈值和规则，就只能重算引用该阈值的结论；“V-13 负载上升”还需要流体模型、经验规则或仿真器。工程本体可以承载变更范围、依赖关系和结果，但不能从一个属性修改自动创造缺失的物理因果知识。

这类任务适合先做增量影响分析：找出直接依赖阈值的规则，传播到受影响的下游结论，并把超出依赖图或模型范围的部分标为未知。若变更触及非局部耦合、反馈回路或外部仿真，就应升级为完整场景分叉或专门仿真。

### 2.3 应急预案推演

“假设两路市电同时中断，备用发电机启动失败，哪些设备的断电容忍窗口会在 30 秒内被突破？”这是多事实联合改变、带时间约束和安全后果的场景。它通常需要事件序列、设备状态迁移、时间参数和应急规则；一个静态描述逻辑一致性检查不能代替动态仿真。[16][^11]

分叉环境可以让多个预案共享同一个基线而相互隔离；每个分支都应只读运行，禁止直接调用生产 Action。对高风险结果，系统应同时输出模型版本、输入假设、时间窗口、未建模依赖和人工批准状态。

### 2.4 合规审计与历史追溯

审计员可能询问：“在上周三的维护窗口，如果 V-12 没有被手动旁路，按当时版本的规则会得到什么结果？”这首先是历史重建问题：需要确定当时的本体版本、数据状态、规则和事件顺序。一个版本标记不是完整的历史状态，也不保证所有外部数据都能重现。

因此，审计场景的正确输出可能是“可重放”“部分重放”或“证据不足”，而不是强行生成一个确定的过去世界。溯源记录可以说明某条结论由哪个版本、活动、代理人和输入材料产生，但溯源词表本身也不保证记录完整。[13][^12]

## 3 分析路径一：场景分叉

### 3.1 场景对象与假设补丁

本文把一次场景分析表示为：

$$\mathcal{S}=(B,\Delta_H,M,Q,E),$$

其中 $B$ 是可重建的基线状态，$\Delta_H$ 是反事实假设补丁，$M$ 是规则、因果模型或仿真模型的版本集合，$Q$ 是评估任务，$E$ 是证据与治理元数据。场景结果写作：

$$R_H=\operatorname{Eval}(\operatorname{Apply}(B,\Delta_H),M,Q).$$

假设补丁不能只写成 $B\cup\Delta_H$。至少需要区分新增集合 $A$、删除集合 $D$ 和替换映射 $U$：

$$\Delta_H=(A,D,U),\qquad B_H=(B\setminus D)\oplus U\oplus A.$$

其中 $\oplus$ 必须由对象标识、属性键、版本和冲突策略定义。若同一属性同时被删除和替换，或两个假设给出互相冲突的值，系统应返回冲突状态，而不是用集合并运算掩盖差异。[^13]

### 3.2 操作流程

1. **解析基线**：从版本库和数据存储中解析完整的基线状态，记录缺失、延迟和不确定来源。
2. **建立只读分支**：为场景生成独立的分支标识和父基线，不允许调用生产写入 Action。
3. **应用假设补丁**：执行新增、删除和替换，并运行模式定义、类型和冲突检查。
4. **选择评估器**：调用选定的逻辑推理器、规则程序、因果模型或仿真器；不把所有工具都统称为描述逻辑推理。
5. **提取任务结果**：只回答 $Q$ 中声明的问题，区分已推导、未推导、冲突、模型外和证据不足。
6. **比较与归档**：将场景结果与基线结果比较，保存输入补丁、模型版本和输出差异。

分叉的优势是隔离和可重放，而不是“自动得到一个完整可能世界”。如果基线状态缺失、规则未覆盖某个因果链，或仿真模型没有通过验证，分叉只能忠实地返回缺口。OWL 直接语义中的后承也是相对于给定本体和模型的关系，不等于现实世界所有事实的闭包。[12][^14]

### 3.3 不一致与执行安全

反事实补丁可能与基线约束冲突。冲突至少有三种解释：假设与当前数据不相容；假设触发了模型中的禁止状态；或本体缺少表达该状态所需的规则。系统不应把三者统一命名为“逻辑上不可能”。更稳妥的结果类型是 `inconsistent_with_baseline`、`unsupported_by_model` 和 `requires_review`，并保留冲突来源。

场景分叉只承担分析，不直接执行现实 Action。涉及行动时，正文讨论的仍是“如果行动发生，按现有规则会推出什么”，而不是一套已经具备触发、执行和物理反馈能力的现实控制方案。[16]

## 4 分析路径二：增量影响分析

### 4.1 从“变换函数”改为受依赖图约束的重算

局部假设可以触发一组依赖结论的失效和重算。与其把它称为一个与珀尔 `do` 算子同构的“变换函数”，不如定义为：

$$R_H=\operatorname{Eval}(\operatorname{Apply}(B,\Delta_H),M,Q),\qquad \Delta R=R_H\ominus R_0,$$

其中 $\ominus$ 是针对结果类型的差异运算。增量算法只是一种计算 $\Delta R$ 的实现：它根据已登记的依赖边、规则输入和缓存状态，找出需要失效、重算或验证的节点。它不自动说明变量之间存在因果关系，更不自动产生概率分布。

### 4.2 操作流程

1. **定位直接影响**：根据补丁涉及的类型、实例、属性和关系，找到直接引用它们的规则、查询和模型输入。
2. **传播依赖**：沿已登记的依赖图传播失效标记；对递归、聚合、否定和外部调用设置明确的处理策略。
3. **重算局部结果**：在安全的隔离环境中重算受影响结果，并记录使用的规则版本。
4. **检查遗漏风险**：如果依赖图不完整、出现删除传播、反馈回路或外部模型调用，标记结果为部分覆盖或升级全量评估。
5. **形成差异包**：输出新增结论、撤销结论、变更结论、未确定结论及其来源。

数据库增量视图维护和 Datalog 增量物化研究表明，删除与修改需要专门的失效和重算策略，不能用“只沿新增事实向前传播”概括。[8][9][^9] 计算成本与影响范围并不存在无条件的正比关系。成本取决于依赖图质量、规则是否单调、是否存在递归和聚合、缓存命中率、外部模型调用以及冲突处理；这些因素需要基准测试。

### 4.3 何时回退到场景分叉

当影响范围无法可靠估计、依赖图存在缺口、假设涉及多事实联合替换，或结果将用于高风险决策时，应回退到场景分叉、全量重算或专门仿真。增量分析的优势是节省部分重复计算，不是降低验证要求。它可以作为初筛器，但不能用局部重算的成功替代全局覆盖证明。

## 5 两种模式的适用边界

| 维度 | 场景分叉 | 增量影响分析 |
|---|---|---|
| 基线 | 可重建的版本化状态 | 同一基线及其依赖索引 |
| 假设 | 可包含多项新增、删除和替换 | 通常从局部补丁开始 |
| 主要优势 | 隔离、重放、比较不同方案 | 减少已知不受影响部分的重复计算 |
| 主要风险 | 基线缺口、全量评估成本、模型外结论 | 依赖遗漏、删除传播、非单调规则和反馈回路 |
| 结果 | 任务相关结论包，不是现实世界副本 | 差异包及覆盖范围，不是因果效应证明 |
| 高风险场景 | 适合只读预案和审计重放，需额外验证 | 适合初筛，必要时升级全量评估 |

模式选择不应由“全局/局部”两个词自动决定。实施时至少要测量：基线是否完整、依赖图是否覆盖、模型是否经过验证、是否存在删除和非单调规则、外部 Action 是否被隔离、结果延迟是否满足任务要求。不同场景可以先增量分析再分叉复核，也可以直接调用经过验证的物理仿真器。

## 6 工程边界与语义对应

### 6.1 与模态和可能世界的关系

工程场景可以借用“可能世界”作为解释性语言，但它不是对模态语义的完整补全。一个场景只承诺：在指定基线、补丁、模型和任务下，系统计算了某些结果。它不承诺存在一个与刘易斯相似性排序或斯塔尔内克选择函数相同的世界选择机制。[^2]

### 6.2 与时态算子 $\mathrm{F}$ 和 $\mathrm{P}$ 的关系

普赖尔的时态逻辑和普努埃利的程序时态逻辑都要求时间结构、状态序列或路径，以及算子的满足条件。[10][11][^16] 普通版本记录只能定位一个历史节点，不能单独判断任意过去命题或未来命题。因此，历史分析还需要事件和数据，未来推演还需要明确的状态变化与时间条件。

### 6.3 与自演进和治理闭环的关系

反事实场景的结果应当保留假设、模型版本、适用范围和证据，让管理层知道结论在什么条件下成立。它是理解变化后果的辅助工具，不直接修改公司的统一本体，也不直接执行现实 Action。[14][^18] 大语言模型可以帮助整理自然语言假设和解释差异，但不能替代逻辑条件和证据。

### 6.4 与形式语义路线的关系

`IL → FOL → DL → 三元组` 可以作为一条工程组织线，但不是形式语义规定的唯一降维路线。本文提供的是一种外部机制：在受限状态、规则和任务上做场景分析。它不能声称“填补了可能世界语义的全部功能”，只能说明工程系统如何承载其中一部分假设分析需求。

### 6.5 溯因不是反事实的简单反向

反事实分析通常从假设出发问结果；溯因则从观察到的结果出发，寻找能够解释它的候选原因。皮尔士把假设性推断视为一种发现和解释活动；现代知识表示中的溯因系统还需要背景知识、候选生成、排序标准和证据权重。[15][18][^17] 因此，溯因可以与场景分叉共享依赖图，但不能被定义为“反事实推理的反向版本”就完成算法说明。它应输出带权候选和证据缺口，而不是唯一原因。

## 7 结论：把“如果”变成可审计的场景协议

反事实分析的核心不是制造一个包含全部事实的替代世界，而是为一次逻辑推演明确四件事：基线是什么，改动是什么，使用哪些规则，回答哪个问题。场景分叉用于比较不同前提，增量影响分析用于追踪哪些结论会随之改变。

这两条路径都受范围限制。缺失数据不会因为创建了分支而消失，未编码的因果关系不会因为运行了闭包而出现，局部重算也不会自动证明全局覆盖。版本记录、逻辑后承、规则物化和行动说明各自承担不同责任，不能被一个“可能世界”概念统一替代。[^19]

因此，本文的结论是：**把反事实问题写成有基线、有假设、有规则范围的逻辑场景，并明确记录差异、未知、冲突和证据。** 这使“如果”能够被复查和比较，同时诚实承认逻辑分析没有解决模型之外的因果与物理问题。

---

## 参考文献

[1] Lewis, D. *Counterfactuals*. Cambridge, MA: Harvard University Press, 1973.

[2] Stalnaker, R. C. “A Theory of Conditionals.” In: Rescher, N. (ed.), *Studies in Logical Theory*. American Philosophical Quarterly Monograph Series, Vol. 2. Oxford: Basil Blackwell, 98–112, 1968. A stable reprint is indexed under DOI: 10.1007/978-94-009-9117-0_2.

[3] Pearl, J. *Causality: Models, Reasoning, and Inference*, 2nd ed. Cambridge: Cambridge University Press, 2009. DOI: 10.1017/CBO9780511803161

[4] Pearl, J. “Causal Diagrams for Empirical Research.” *Biometrika*, 82(4): 669–688, 1995. DOI: 10.1093/biomet/82.4.669

[5] Blackburn, P., de Rijke, M. and Venema, Y. *Modal Logic*. Cambridge: Cambridge University Press, 2001. DOI: 10.1017/CBO9781107050884

[6] Alchourrón, C. E., Gärdenfors, P. and Makinson, D. “On the Logic of Theory Change: Partial Meet Contraction and Revision Functions.” *The Journal of Symbolic Logic*, 50(2): 510–530, 1985. DOI: 10.2307/2274239

[7] Arlat, J. et al. “Fault Injection for Dependability Validation: A Methodology and Some Applications.” *IEEE Transactions on Software Engineering*, 16(2): 166–182, 1990. DOI: 10.1109/32.44380

[8] Gupta, A., Mumick, I. S. and Subrahmanian, V. S. “Maintaining Views Incrementally.” In: *Proceedings of the 1993 ACM SIGMOD International Conference on Management of Data*, 157–166, 1993. DOI: 10.1145/170035.170066

[9] Motik, B., Nenov, Y., Piro, R. and Horrocks, I. “Incremental Update of Datalog Materialisation: The Backward/Forward Algorithm.” *Proceedings of the AAAI Conference on Artificial Intelligence*, 29(1), 2015. DOI: 10.1609/aaai.v29i1.9409

[10] Prior, A. N. *Past, Present and Future*. Oxford: Clarendon Press, 1967. DOI: 10.1093/acprof:oso/9780198243113.001.0001

[11] Pnueli, A. “The Temporal Logic of Programs.” In: *18th Annual Symposium on Foundations of Computer Science*, 46–57, 1977. DOI: 10.1109/SFCS.1977.32

[12] W3C OWL Working Group. *OWL 2 Web Ontology Language: Direct Semantics (Second Edition)*. W3C Recommendation, 11 December 2012. https://www.w3.org/TR/owl2-direct-semantics/

[13] W3C. *PROV-O: The PROV Ontology*. W3C Recommendation, 30 April 2013. https://www.w3.org/TR/prov-o/

[14] National Institute of Standards and Technology. *Artificial Intelligence Risk Management Framework: Generative Artificial Intelligence Profile*. NIST AI 600-1, 2024. DOI: 10.6028/NIST.AI.600-1

[15] Peirce, C. S. “Deduction, Induction, and Hypothesis.” *Popular Science Monthly*, 13: 470–482, 1878.

[16] Reiter, R. *Knowledge in Action: Logical Foundations for Specifying and Implementing Dynamical Systems*. Cambridge, MA: MIT Press, 2001.

[17] Avizienis, A. et al. “Basic Concepts and Taxonomy of Dependable and Secure Computing.” *IEEE Transactions on Dependable and Secure Computing*, 1(1): 11–33, 2004. DOI: 10.1109/TDSC.2004.2

[18] Kakas, A. C., Kowalski, R. A. and Toni, F. “Abductive Logic Programming.” *Journal of Logic and Computation*, 2(6): 719–770, 1992. DOI: 10.1093/logcom/2.6.719

---

## 脚注

[^1]: “可能世界被 FOL 整体拿掉”是对选定语言签名的描述，不是对 FOL 表达能力的全称判断。通过把世界和时间作为个体、引入可达或状态关系，可以在 FOL 中显式表示相关结构。[5]

[^2]: Lewis 的相似性排序与 Stalnaker 的选择函数给出条件句的语义机制；本文只借用“相对于基线选择情形”的直觉，不把版本分支宣称为其中任一种语义的实现。[1][2]

[^4]: 模态逻辑的标准翻译把世界或状态作为一阶结构中的对象，并用关系表示可达性；这种翻译说明“没有内置模态算子”和“不能表示世界”不是同一个命题。[5]

[^5]: Lewis 的 *Counterfactuals* 与 Stalnaker 的条件句理论分别采用相似性排序和选择函数。二者的世界选择原则需要额外的语义结构，不能由一个基线快照和一个时间戳自动给出。[1][2]

[^6]: Pearl 的 `do` 操作在结构因果模型中改变变量的生成方程或切断入边；把事实字段改写后沿普通规则传播，只有在因果模型和干预语义另行定义时才可称为因果分析。[3][4]

[^7]: AGM 理论区分 expansion、contraction 和 revision。本文借用“新增、删除、替换必须分开记录”的工程原则，不把 AGM 公设直接当作数据库回滚算法。[6]

[^8]: Arlat 等人把故障注入作为可控的可靠性验证方法；Avizienis 等人则从系统属性和故障、错误、失效的分类讨论可依赖性。正文只据此说明故障注入和仿真是独立验证活动，不声称它们自动提供因果真值。[7][17]

[^9]: 增量视图维护和 Datalog 增量物化研究了派生结果如何随插入、删除和修改更新。正文只使用其计算背景，不把增量传播等同于 Pearl 因果传播。[8][9]

[^10]: 第2节的 V-12、B-3 和数据中心均为说明协议边界的设计例子；它们不代表真实客户数据、实际安全阈值或性能基准。

[^11]: 动态应急推演需要状态、事件、时间窗口和路径评价。Reiter 的行动理论说明，动作描述、状态变化和执行语义是需要分别规定的层次。[16]

[^12]: PROV-O 提供 Entity、Activity 和 Agent 等溯源概念，可用于记录场景输入、模型运行和批准活动；使用词表不等于记录已经完整。[13]

[^13]: `B_H=(B\\setminus D)\\oplus U\\oplus A` 是本文的补丁语义示意。实际系统还需要定义键冲突、版本优先级、删除传播、默认值和撤销策略；它不是某个现有逻辑或数据库标准的直接公式。

[^14]: OWL 直接语义中的后承关系相对于指定本体和模型定义；即使推理器完成某个任务，也不能推出所有外部事实或物理状态。[12]

[^16]: Prior 的时态逻辑和 Pnueli 的程序时态逻辑都要求时间结构、状态或路径及其满足条件。快照之间的版本关系和时间戳只能支持追溯定位，不能单独解释 $\mathrm P$ 或 $\mathrm F$。[10][11]

[^17]: Peirce 把 hypothesis 视为从观察结果出发提出可解释假设的推断活动；Kakas、Kowalski 与 Toni 将溯因放入逻辑程序中的候选解释和推理框架。溯因因此不是反事实语义的简单逆运算。[15][18]

[^18]: NIST AI 600-1 的风险管理框架强调信息完整性、验证、持续监测和治理。场景结果进入高风险决策前，应有模型、数据和责任链审查。[14]

[^19]: 本文所说的“可审计”是工程属性：结果可定位到基线、补丁、模型、任务和运行记录。它不等于结果在现实世界中必然正确，也不等于所有未知项已经消除。
