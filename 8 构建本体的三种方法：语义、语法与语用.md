# Three Methods of Ontology Construction: Semantics, Syntax, and Pragmatics

**构建本体的三种方法：语义、语法与语用**

---
作者：顾昊



\medskip

\noindent\textbf{Keywords:} ontology construction $\cdot$ semantics $\cdot$ syntax $\cdot$ pragmatics $\cdot$ Morris's trichotomy $\cdot$ category theory $\cdot$ large language models $\cdot$ field deployment engineer $\cdot$ data annotation $\cdot$ structural derivation $\cdot$ distributional semantics

\medskip

\noindent\textbf{AMS Subject Classification:} 68T30 (Knowledge representation) $\cdot$ 68T50 (Natural language processing) $\cdot$ 03B70 (Logic in computer science) $\cdot$ 18A15 (Foundations of category theory)

---


## 摘要

前七篇论文从不同角度使用了莫里斯（Morris）的符号学三分法——语法、语义、语用——来解释本体论各层面的问题：语义检验对内涵性算子的失效、语用检验作为最终判据、符号圈的三层结构。但一个基础性问题尚未被正面回答：**本体本身是如何被构建出来的？** 本文提出，本体的构建恰好对应三种不可互相替代的方法——它们分别以莫里斯的三个维度作为其操作根基。语义方法以 FDE（前线部署工程师）的业务理解和数据标注为核心——由理解现场的人为符号赋予确定含义。语法方法以范畴论的结构性推导为核心——一个对象由它与所有其他对象的关系总和唯一确定，不需要标注便可通过关系网络锁定其本体位置。语用方法以大语言模型的分布语义为核心——从海量语料中的使用模式归纳出符号的含义，作为大规模候选的生成器。三种方法不是竞争性的——它们分别解决了本体构建中的精度问题（语义）、规模问题（语法）和覆盖问题（语用）。本文论证了三种方法各自的不可替代性，以及它们在工程本体论完整构建流程中的位置：语用提议、语义确认、语法固化。


## 1 引言：本体是怎么被造出来的？

前七篇论文讲了本体论是什么、谓词怎么消解、符号圈怎么自演进、垂类怎么选范式、Montague 怎么继承、反事实推理怎么做、数字孪生怎么交付。但有一个问题一直没有被单独拿出来讨论：**谁来造本体？怎么造？**

这不是一个细枝末节的操作性问题。它的答案决定了整个体系的可操作性和可规模化。一个在理论上自洽的工程框架，如果构建过程依赖天才、不可复制、不可分工，那就不是一个工程框架——它是一个个人手艺。

莫里斯的三分法——语法、语义、语用——在前七篇论文中被反复调用。第二篇用它来分析为什么 $\lozenge$ 和时态在语义层难以闭合，第三篇用它来构建符号圈的三层检验，第四篇用它来论证符号学模型对垂类 LLM 的不可替代性。但在这所有的使用中，三分法始终是一个**分析工具**——它被用来解释"为什么某个东西好做、某个东西难做"，而不是被用来指导"怎么做"。

本文的论点是：莫里斯的三分法恰好对应**三种构建本体的工程方法**，而且这三种方法在同一个工程流程中缺一不可。

- **语义方法**：本体由理解业务的人——FDE（前线部署工程师）——通过数据标注的方式直接构建。符号的含义来自标注者的语义判断。这是精度最高的方法——每一个赋值都有人的权威背书——但它无法规模化。
- **语法方法**：本体通过结构性推导来自动构建——一个对象在本体中的位置和类型，由它与所有其他对象之间的关系总和唯一确定。这是范畴论原则的工程应用：对象不由它"是什么"来定义，而由它"与什么发生关系"来定义。这是规模化最高且保持确定性的方法——但它的前提是一个足够稠密的关系网络作为推导基础。
- **语用方法**：本体通过大语言模型从海量语料中的使用模式来归纳候选——"阀门"在操作规程中如何被使用、跟哪些动词共现、被哪些约束条件修饰、在哪些工作流中出现。LLM 从这些分布模式中归纳出一个符号的语义候选。这是覆盖率最高的方法——几乎所有可以从语料中学到的东西，LLM 都能提议——但它完全没有确定性保证，输出只能作为候选，不能直接固化。

三种方法对应本体构建中的三种核心需求：**精度**（你造的东西对不对）、**规模**（你能不能造出足够多的东西）、**覆盖**（你能不能发现你还不知道的东西）。这三者之间的张力是本体构建的核心工程张力——而本文的论证目标正是展示：三种方法不是三个可选方案，而是必须同时存在于同一个构建流程中的三种不可替代的能力。


## 2 语义方法：FDE 的标注与语义权威

### 2.1 FDE 是谁

FDE（前线部署工程师）不是在办公室里画 UML 的架构师。他是被派到客户现场的资深程序员——他打开过设备柜的门，摸过结了垢的管道，翻过三年前的巡检记录，跟操作员一起值过夜班。当操作员说"V-12 有时候不太对劲"时，他知道"不对劲"在工程语言里是"温度波动超过正常工作范围 15%、持续超过 30 秒"。当客户的技术人员拿出一张 P&ID 图纸说"这就是我们的工艺流程"时，他知道图纸上的某些管线编号在现场已经被改过——因为他在车间里走的时候看到过改造记录。

FDE 的核心能力不是写代码。他当然会写代码——但每个程序员都会写代码。FDE 的核心能力是**把一个混乱的、充满本地术语和历史遗留的现场图景，翻译为一套干净的、可被系统理解的工程概念**。翻译不是语言转换——翻译是理解。FDE 之所以能做这个翻译，恰恰是因为他不只在办公室里看文档——他在现场。

### 2.2 FDE 如何用语义方法构建本体

FDE 构建本体的方式是最直接、最传统、也是最不可替代的：**他把对业务的理解，通过数据标注的方式，固化为本体中的符号和约束。**

具体来说：

1. FDE 在现场走访中理解客户的业务现实——哪些实体存在（ObjectType）、它们之间有什么结构关系（LinkType）、它们的状态可以被哪些数据描述（PropertyType）、什么样的状态变化应该引起什么操作（ActionType）。
2. 在初步理解的基础上，FDE 开始标注——不是标注训练数据，而是标注本体结构。他为每一个 ObjectType 填写实例示例——"在你的系统中，哪种具体的设备属于这个类型？给我看一个真实的设备编号。"他为每一条 Link 寻找现场证据——"你说这条管道连接 V-12 和 V-13——现场有没有这条管道的标签或者设备台账记录？"他通过交叉验证来确认——"你说这个阀门的温度传感器在 SCADA 中，那 MES 中同一台设备的温度字段来自哪里？"
3. 在标注的过程中，FDE 不断修正自己的理解。他发现客户说的"阀门"其实有两种——手动阀和自动阀，它们在业务逻辑上的处理方式完全不同。他发现某个属性在配置文档中是必填的，但在实际系统中从未被使用——这意味着配置文档的规定在生产中已经被废弃了，但从未正式通知过任何人。标注的过程，就是 FDE 的理解从模糊走向清晰、从假设走向确认的过程。

这种方法的本质是：**本体的语义权威是人类判断。** 每一条类型定义、每一条 Link、每一条 Property 的约束——它们之所以是正确的，不因为它们在逻辑上自洽，而因为一个理解现场的人为它们做了背书。

### 2.3 语义方法的不可替代性

语义方法的效率是低的——一个有经验的 FDE 一周能完整覆盖二十到三十个 ObjectType 的本体定义，大型工业系统可能需要数百个类型。而且它不易复制——换一个 FDE，可能对同一个现场有不同的理解深度。

但它是**不可替代的**。因为语法方法推导出的结论需要人来判断是否正确——"这个推导说他俩应该是一条管道连接的关系，但你亲眼看过现场——他俩之间实际有管道吗？"因为 LLM 从语料中提取的候选需要人来确认——"LLM 说'泵'在本体中应该有属性'max_flow_rate'——但在客户的业务语境中，他们对泵真正关心的属性是'是否在维护窗口内'。"语义方法的不可替代性来自于一个简单的事实：**工业现场的真实，最终只能由到过现场的人来确定。** 逻辑可以检验一致性，统计可以归纳概率，但真假——这个设备到底是不是在那里、这个关系到底是不是真实存在的——只有 FDE 的判断是最终的锚点。


## 3 语法方法：范畴论的结构性推导

### 3.1 一个对象由它的全部关系来定义

语义方法的前提是：有一个人告诉你"这是阀门""那是管道"。但有一个更深层的问题：在没有人告诉你的情况下，一个对象在本体中的位置是否可以纯粹通过它与其他对象的关系被推导出来？

范畴论的答案是：**可以。** 范畴论不需要问"这个对象是什么"——它问的是"这个对象与其他对象之间有多少个箭头、这些箭头指向什么、这些箭头满足什么交换律"。给定一个对象集合和它们之间的态射（关系箭头），每个对象的身份不是由它的内部属性决定的，而是由它与集合中其他所有对象的态射图样**唯一确定**的。这个原则的严格表述是 Yoneda 引理：一个对象被它到所有其他对象的态射（以及从所有其他对象到它的态射）完全决定。你在一个系统中知道一个东西与所有其他东西的所有关系，你就不需要再给它一个名字——它的名字就是这些关系的总和。

### 3.2 语法方法在本体构建中的应用

在本体构建中，语法方法不需要人标注"这个对象是 `Valve`"。它只需要知道这个对象的以下信息：

- 有哪些 Link 从它出发、指向什么类型的对象
- 有哪些 Link 指向它、从什么类型的对象出发
- 它被哪些 Action 操作
- 它触发了哪些 Action 的副作用
- 它被哪些 InterfaceType 约束

把所有这些结构线索汇集在一起，一个对象的"语法身份"就被唯一确定了。如果一个对象被一个 `connects_to` Link 指向、从一个 `connects_to` Link 出发、拥有 Property `temperature` 和 `valve_status`、在 Action "超温关阀" 中作为被操作对象出现——那么不管你怎么称呼它，它在系统中的角色是阀门。换句话说：**不是标注告诉系统它是阀门——是它在整个关系网络中的位置告诉系统它是阀门。**

第五篇论文（《蒙太格的遗产》）已经为这个方法提供了理论基础。Montague 的组合性原则——复杂表达式的意义由组成部分的意义和组合规则唯一确定——在这里被推广到了本体构建层面：一个对象的**本体类型**由它参与的**所有关系的组合**唯一确定。这是组合性原则最激进的工程应用——不仅句子的语义由组合决定，系统中的每一个实体是什么，也由它在系统中的全部关系来决定。

### 3.3 语法方法的工程条件

语法推导的前提是：关系网络足够稠密。如果系统只知道一个对象有 `temperature` 而没有其他任何 Link 或 Action 关联，它的语法身份是无法被锁定的——有温度的东西太多了。推导的确定性随关系密度的增加而收敛：一条 Link 只能排除许多可能，两条 Link 可以缩小范围，三条以上的正交关系足够唯一确定一个工业对象的本体类型。

在工程实践中，这意味着语法方法并不要求"先把所有关系都建好再用"——它是逐步生效的。本体构建的早期，关系稀疏，语法推导只能给出"可能是 A 或 B 或 C"的建议。随着更多 Link、Property 和 Action 被接入，候选范围越来越窄，最终收敛到一个唯一的类型。这个收敛过程本身还起到了一个重要的校验作用：如果一个对象的关系图样最终无法收敛到任何已定义的 ObjectType，这不意味着推导失败——它意味着系统发现了一个需要定义的、不在现有本体中的真实事物类型。

### 3.4 语法方法与第六篇的关联

第六篇（反事实推理）中的世界分叉和变换函数，都依赖一个共同的前提：推理规则的输入对象已被正确识别。如果推理规则"超温关阀"不知道它的 `target` 是不是阀门，规则无法安全执行。语法方法为这个前提提供了自动化保障——它保证每个被推理规则引用的对象，都有一条可追溯的结构证据链证明它就是规则认为它是的那个类型。这不仅是构建时期的便利——它是运行时的安全保障。


## 4 语用方法：LLM 从使用中归纳语义

### 4.1 "意义即使用"的工程实例化

维特根斯坦后期哲学的核心命题——"意义即使用"——在前七篇论文中多次被提到，但从未被正面展开为一个工程方法。语用方法正是对这个命题的工程实例化：不是问"本体中的 `Valve` 应该被如何定义"，而是喂给 LLM 一万份工业操作规程、设备手册、巡检记录和故障分析报告，让它回答：**在这些文本中，"阀门"是怎么被使用的？**

LLM 不能给出一个形式化的定义——定义是标注者（FDE）的工作。但它能给出大量从使用中浮现的语义线索：阀门总是跟"压力""温度""开度""密封"这些词共现，阀门在操作流程中总是出现在"关闭 → 确认关闭 → 后续操作"这个序列中，阀门的故障描述总是围绕"卡涩""泄漏""误动作"展开，阀门的维护总是在"年度大修"或"计划停机"的上下文中被提及。这些不是形式定义——它们是使用模式。但这些使用模式的密度和规律性，足以让 LLM 提议出：本体中应该有一个 ObjectType 叫 `Valve`，它应该有一个 Property `status` 取值为 `OPEN/CLOSED/FAULT`，它应该有 Property `seal_pressure` 和 `actuation_type`，它应该在 Action "shutdown_procedure" 和 "annual_overhaul" 中被引用。

### 4.2 语用方法的工程角色：不是裁判，是提议者

语用方法的最大优势是覆盖率。FDE 一周能理解二三十个类型，LLM 几小时可以处理数千页文档，提取数百个候选概念。但它的最大弱点同样是致命的：它完全没有确定性保证。LLM 的语义归纳是概率性的——它可能把"泵"和"压缩机"在语义空间中混为一谈（因为它们的使用语境高度重叠），可能从一份已经过时的操作手册中提取了已被替换的设备类型，可能在罕见术语上给出完全偏颇的语义理解。

因此，语用方法在本体构建流程中的工程角色是明确的：**它从来不是裁判——它是提议者。** LLM 的输出不直接进入本体。它必须经过三层检验——这是第三篇（符号圈）中已经建立的流程——语法层检查 LLM 提议的 Property 数据类型是否与已有本体结构相容，语义层检查提议的 ObjectType 是否与 FDE 的现场理解一致，语用层检查提议在真实的业务场景中是否"说得通"。只有通过全部三层检验的候选，才能被固化到本体中。

但这丝毫不削弱语用方法的价值。在 FDE 到达现场之前，LLM 已经可以从客户提供的技术文档中批量提取候选概念——FDE 到了之后不是从零开始理解业务，而是在一个已经被 LLM 预填充的候选框架上开始验证、修正和细化。FDE 的角色从"创作者"变成了"策展人"——不是从空白开始，而是在一个有信息量的起点上工作。这个转变是可规模化的前提：一个 FDE 能同时维护的现场数量，从三个变成十个。

### 4.3 语用方法与数据中台的关联

第七篇（数字孪生）提出的数据中台，其建设过程中最消耗人力的环节——跨系统实体统一——正是语用方法的天然应用场景。当数据中台需要判断 SCADA 的 `V-12`、MES 的 `VALVE_B3_012` 和维护系统的 `EQ-2021-0457` 是否指向同一个真实实体时，LLM 可以同时检查三类线索：标识符的命名规范（位号规则）、关联数据的交叉验证（这三条记录关联的管道位号是否一致）、以及非结构化文本中的上下文（在巡检记录中引用这些标识符时，描述的地理位置和行为是否一致）。LLM 的判断不能替代人工确认——但它在人工确认之前，已经将数百个候选配对缩小到了几十个需要重点关注的疑似同一实体组。


## 5 三种方法的不可替代性与协同

### 5.1 各自对应一种工程需求

三种方法不是三个可选方案——它们各自解决本体构建中的一种不同的工程需求：

- **语义方法解决了精度问题**：本体对不对。FDE 在标注中的每一次判断，都是对本体精确性的一重保障。这个保障不可自动化——它依赖的是对工业现场的亲身理解，而不是对逻辑规则或统计分布的计算。
- **语法方法解决了规模问题**：本体能不能被建得足够大。如果每一个实例都需要手动标注，一千个 ObjectType 需要 50 个 FDE 工作六周。语法推导可以在关系网络就位之后，自动化地为大量实例确定其在本体中的位置。它是从"手工"到"规模"的跳跃。
- **语用方法解决了覆盖问题**：本体有没有遗漏。FDE 理解的是他已经看到的东西，语法推导锁定的是已经被关系网络覆盖的东西。LLM 的价值在于发现那些两者都未能覆盖的概念——从海量文本中捡出 FDE 从未接触过、关系网络尚未涉及的语义线索。

### 5.2 三者的协同流程

在完整的本体构建工程中，三者的协作流程是：

1. **语用提议**：LLM 从客户的技术文档、操作规程、巡检记录中批量提取候选概念和候选关系。输出是一个尚未验证的本体框架草案。
2. **语义确认**：FDE 带着这份草案到现场，逐一验证、修正、细化。确认后的本体结构具有语义权威——每个符号的含义都有人的背书。
3. **语法固化**：在 FDE 确认的本体结构基础上，语法推导自动将结构约束传播到所有实例——每个实例的类型和关系通过其关系网络被唯一确定。同时，语法推导持续运行一致性检查——任何违反类型约束或结构规则的操作都会被标记。

这不是一个线性的瀑布流程。在实际工程中，三者是持续交互的。FDE 在确认过程中发现新的事实，这些事实反馈回 LLM 的语料中，影响下一轮候选提取。语法推导发现了一个对象的关系图样与已定义的类型都不匹配——这触发了 FDE 去现场确认是否存在一个新的实体类型。在语料中频繁出现但 FDE 从未实际接触过的概念被标记为"待现场确认"。

### 5.3 与莫里斯三分法的精确对应

现在可以补齐这个架构的理论渊源。本文与第四篇（符号学作为垂类模型唯一解）在同一个理论地基上，但开掘的是不同的层面。第四篇用莫里斯的三分法来论证**检验方法**——什么样的命题用什么样的检验。本文用莫里斯的三分法来论证**构建方法**——什么样的本体用什么样的方法来造。

| 莫里斯的维度 | 构建方法 | 操作方式 | 角色 | 解决的问题 |
|---|---|---|---|---|
| 语义 | FDE 标注 | 人对业务的直接理解 | 语义权威 | 精度 |
| 语法 | 范畴论推导 | 关系网络唯一确定对象 | 结构保障 | 规模 |
| 语用 | LLM 归纳 | 从语料使用模式提取候选 | 提议引擎 | 覆盖 |

这个对应不是偶然的。莫里斯的三种符号关系——符号与对象（语义）、符号与符号（语法）、符号与使用者（语用）——恰好对应了本体构建中的三种信息来源：人的理解、结构的约束、和语言的使用习惯。三个维度在这组论文中第一次不是作为"分析框架"出现，而是作为"操作方法"出现——它们不是用来事后解释一个本体的优劣，而是用来指导本体的构建流程本身。


## 6 结论：三种方法，一个流程

本文回答了前七篇论文悬置的一个基础性问题：本体是怎么被造出来的？答案是：它有三种造法，缺了哪一种都不够。

**语义方法**——FDE 在前线理解业务、标注数据、为符号赋予确定含义——这是精度最高的方法，是整个本体可靠性的最终锚点。但它不能规模化，它的代价是时间和对 FDE 资深经验的依赖。

**语法方法**——通过一个对象与所有其他对象的关系总和来唯一确定它的本体位置——这是规模化的结构性保障。当关系网络足够稠密时，语法推导可以在不依赖人工标注的情况下，为大量实例自动锁定其类型。但它的前提是关系网络的存在——在构建初期，关系稀疏，语法推导能提供的是建议而非结论。

**语用方法**——通过 LLM 从海量语料中归纳使用模式来提议候选概念和候选关系——这是覆盖率的保障。LLM 不能赋予任何符号确定性，但它能告诉 FDE 和语法推导系统：这里可能有什么东西值得一看。

三种方法在完整的构建流程中各占一个位置：语用提议，语义确认，语法固化。在这个意义上，本文不仅回答了"本体怎么造"的问题——它还完成了一个从第四篇开始的理论收束：莫里斯的三分法不仅是一个检验框架（第四篇的用法），也是一个构建框架（本文的用法）。同一组理论装置，在工程本体论的两个不同层面——检验与构建——分别完成各自的工作。

---

作者：顾昊
