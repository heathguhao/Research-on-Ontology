# Possible Worlds and Counterfactual Reasoning: The "What If" in Engineering Ontology

**可能世界与反事实推理：工程本体论中的"如果"**

---
作者：顾昊



\medskip

\noindent\textbf{Keywords:} possible worlds $\cdot$ counterfactual reasoning $\cdot$ modal logic $\cdot$ snapshot forking $\cdot$ transform function $\cdot$ engineering ontology $\cdot$ what-if analysis $\cdot$ simulation $\cdot$ intensional logic $\cdot$ abduction

\medskip

\noindent\textbf{AMS Subject Classification:} 03B45 (Modal logic) $\cdot$ 03B70 (Logic in computer science) $\cdot$ 68T30 (Knowledge representation) $\cdot$ 68U20 (Simulation) $\cdot$ 03A05 (Philosophical logic)

---


## 摘要

蒙太格语法以降维路线中的第一站——从内涵逻辑（IL）降为一阶逻辑（FOL）——所付出的第一个、也是最沉重的代价，就是可能世界语义的放弃。可能世界被 IL 用作处理内涵性语境（命题态度、时态、模态）的核心装置，但在 IL → FOL 的降维中被整体拿掉了——因为在 FOL 中不存在对"世界"的显式指称。这一放弃换来了半可判定推理，但同时也留下了一个空洞：工业系统在失去可能世界之后，如何完成"如果……会怎样"的推理？本文的核心论题是：可能世界语义可以从 IL 的推理引擎中被移除——因为不可判定——但"反事实推理"作为一种工程能力不能被移除。工程本体论需要的不是在推理引擎里重建可能世界，而是在系统架构中为反事实推理提供两种工程安全的实现方案——**世界分叉方案**（将反事实假设作为对当前本体快照的分叉，在分叉世界内运行全部现实推理规则，得到一个反事实可能世界作为推演结果）和**变换函数方案**（将反事实假设作为一个作用于现实事实上的变换函数，计算受影响的属性或链接的增量变化，投射为局部可能世界结果）。两种方案不是竞争性的——它们分别对应两种不同的工程场景：分叉方案适合全局仿真和应急预案推演（"如果整个设备组 B-3 断电，上下游会受到什么影响"），变换方案适合局部影响分析和设计变更评估（"如果把 V-12 的触发阈值从 300°C 改成 250°C，告警频率会有什么变化"）。本文论证了两种方案的工程边界条件，并指出它们共同填补了 IL → FOL 降维时放弃可能世界语义所遗留的工程功能空洞——不是在推理引擎里复活不可判定的可能世界，而是在推理引擎外构建可判定的反事实工程机制。


## 1 引言：IL → FOL 时我们丢掉了什么

《本体论是什么》和《蒙太格的遗产》共同建立了从内涵逻辑（IL）到一阶逻辑（FOL）到描述逻辑（DL）再到三元结构的完整降维路线。这条路线上的每一步都是主动放弃一部分表达力来换取一部分可判定性。在 IL → FOL 这一步，放弃的最大一块表达力，就是**可能世界语义**。

Montague 的内涵逻辑用可能世界语义来处理自然语言中最棘手的一类现象——内涵性语境。"John believes that Mary is happy"——这句话的真值不能仅由"Mary is happy"在当前世界中的真值决定，因为它报告的是 John 的信念状态，而 John 的信念可能和现实不一致。IL 的解决方案是：让"Mary is happy"的语义值成为一个从可能世界到真值的函数（即内涵），然后让"John believes"操作的是这个函数，而不是它在当前世界中的外延值。

IL → FOL 的降维拿掉了这一切。FOL 没有可能世界，没有内涵算子，没有从世界到外延的函数。一个谓词在 FOL 中的语义值是它在当前（且唯一）世界中的外延。这个降维的代价是明确的：系统失去了表达"在另一种情况下，某件事会意味着什么"的能力。换来的是推理在半可判定底线上的可终止性。

《蒙太格的遗产》在第四章第三节中用一个表格概括了每一步降维的得失——IL → FOL 这一行的备注是"第三重承诺（可能世界语义）的工程牺牲"。这个备注是准确的：可能世界被牺牲了。但它没有回答一个接踵而来的问题：**牺牲之后，谁来替代？**

本文的回答是：可能世界不能被 IL 的逻辑装置替代——因为那个装置是不可判定的——但可能世界的**工程功能**可以被重建。不是重建 IL 的可能世界语义，而是重建工业系统在做"如果……会怎样"时需要的那类推理——**反事实推理**。IL 的可能世界是对自然语言中内涵性语境的语义解释装置；工程本体论的反事实推理是对工业场景中假设性推演的决策支持装置。两者服务于不同的目的，使用不同的机制，面对不同的约束——但它们在"系统性的假设空间中运行推理"这一结构特征上是同源的。

### 1.1 前人的相关工作与本文的定位

反事实推理不是一个新问题。在哲学的领域中，Lewis（1973）的反事实语义和 Stalnaker（1968）的选择函数语义已经为这个问题提供了经典的形式框架。在人工智能的领域中，Pearl（2000, 2009）的结构因果模型和 do-calculus 提供了面向概率系统的反事实推理机制。在软件工程的领域中，故障注入（fault injection）作为安全关键系统的标准验证方法，已经以另一种形式实践了"假设 X 失效，然后推理系统行为"的工程流程。本节简要梳理这三条前驱工作的核心思想，并明确本文相对于它们的位置。

**Lewis 的反事实语义：可能世界相似性排序。** Lewis（1973）的反事实条件句语义有一条精确的核心操作：$\phi \ \square\!\rightarrow \psi$ 在可能世界 $w$ 中为真，当且仅当：不存在一个 $\phi \land \lnot \psi$ 成立的可能世界比某个 $\phi \land \psi$ 成立的可能世界更接近 $w$。换言之，在与现实最相似的可能世界中，如果前件成立则后件成立。Lewis 的反事实语义与本文的世界分叉方案在操作的抽象层次上有深刻的结构同源：Lewis 的"最相似的可能世界"对应于本文的基线快照——除反事实修改外其余事实与基线完全相同；Lewis 的"前件成立"对应于本文的反事实假设注入；Lewis 的"后件成立"对应于本文的推理闭包中的可推导结论。然而，Lewis 的可能世界是一个哲学概念，不附带任何可判定性保证——"相似性"本身是一个开放式的哲学判断，而本文的"相似性"被快照机制的版本边界严格定义。从这一角度说，本文的世界分叉方案是 Lewis 反事实语义在工程本体论中的**可判定实例化**。

**Pearl 的 do-calculus：面向概率因果的变换算子。** Pearl（2000, 2009）的结构因果模型（SCM）和 do-operator 在操作结构上与本文的变换函数方案高度相似。$do(X = x)$ 的操作是将变量 $X$ 从因果图中割断其所有入边，强制赋值为 $x$，然后计算其余变量的新概率分布。这与本文的"将反事实假设作为对特定事实的修改，沿推理规则的前向链计算增量变化"在操作逻辑上是同构的。然而，两者面向完全不同的因果域：Pearl 的 do-operator 面向统计因果——它需要先有完整的因果图和条件概率表，输出的是其余变量的概率分布变化；本文的变换函数面向逻辑因果——它需要的是可判定的推理规则和受影响的事实子集，输出的是其余事实的确定性变化。两者不是竞争关系，而是同一种操作模式在两种不同因果域上的分别实现。

**AGM 信念修正：不一致性的处理哲学。** Alchourrón、Gärdenfors 和 Makinson（1985）提出的 AGM 理论处理的核心问题是：当一个知识库收到一条与当前知识不一致的新信息时，如何以最小代价恢复一致性。AGM 提出了八条理性公设，约束了"收缩"和"修正"两种操作——其核心预设是系统应该**主动修正知识库**以恢复一致性（最小改变原则）。本文在 §3.3 中提出了一个不同的工程立场：在反事实推理的语境中，不一致性不是一个需要被修复的故障——反事实世界是一次性的、推演完就销毁的，不一致性只是一个信息："这个假设在给定推理规则下不可实现。"AGM 的修正操作适用于需要持续更新知识的系统（如数据库），而工程本体的反事实推理不需要把不一致性作为需要消除的障碍。

**故障注入：反事实推理的工程前身。** 在安全关键系统的工程实践中，故障注入是一种标准的验证方法——在系统的仿真副本中主动注入故障条件（传感器失效、通信中断、电源波动），观察系统的保护性反应。本文的 §2.1 描述的故障仿真场景，正是故障注入的标准用例。本文相较于故障注入的推进在于：故障注入注入的是系统**已知**的故障模式（不需要"假设"未知的因果链），而本文的变换函数方案可以处理"超出已知推理规则边界"的假设；此外，本文将故障注入的工程实践提升到了本体论的理论层面——世界分叉不再是一个专门的测试工具，而是本体系统的一种通用推理能力。

本文在第二节论证工程系统为何需要反事实推理——四种典型场景。第三节提出第一种方案：世界分叉方案。第四节提出第二种方案：变换函数方案。第五节比较两种方案的适用边界。第六节将反事实推理接入当前的论文序列——包括与模态可能、时态、符号圈、IL降维的关系，以及溯因推理作为反事实推理的反向版本。


## 2 工程系统为什么需要反事实推理：四种典型场景

在进入具体的方案设计之前，有必要先明确反事实推理在工业系统中对应的真实需求。以下四个场景代表了四种不同的工程判断类型，每一种都需要系统知道"如果不是现在这样，而是那样，会发生什么"。

### 2.1 故障仿真

系统当前状态：V-12 阀门在 320°C 时正常关闭。工程师提出反事实假设："假设 V-12 的温度传感器在 290°C 时读数偏移 +15°C（即实际温度已达 305°C 但传感器读数为 290°C），系统行为会怎样？"

这不是一个可以被观测的状态——因为传感器失效本身意味着系统无法从传感器读数中推断真实温度。但工程师需要知道：在这个失效模式下，V-12 是否会在实际超温时未能关闭？如果会，安全裕度是多少？

这种推理不能被 DL 的公理所覆盖，因为 DL 的公理预设了传感器读数是真实的——公理写的是"当读数超过 320°C 时，关闭阀门"，而不是"当实际温度超过 320°C 但读数可能不准确时，关闭阀门"。让每一个公理都显式地考虑传感器失效模式，会使本体膨胀到不可维护的程度。反事实推理提供了一种更轻量的解决方案：在假设空间中临时注入"读数偏移 +15°C"，让系统在这一临时假设下运行推理，观察后果。

### 2.2 设计变更的连锁效应评估

系统当前配置：设备组 B-3 包含六个阀门，每个阀门的触发阈值为 300°C。运维团队提议将 V-12 的触发阈值降到 250°C，以减少该管段的超温风险。在执行这一变更之前，系统需要回答："如果只改 V-12 的阈值，B-3 组内其余阀门的关闭频率是否会因流量再分配而上升？"

这不是一个简单的一阶推理——它涉及物理因果链的推演。阈值降低 → V-12 关闭更频繁 → 流量转移到其余阀门 → 其余阀门的负载上升。当前本体中存储的是各阀门的当前阈值和关闭逻辑，没有任何一个公理可以直接从"V-12 阈值降低"推导出"V-13 负载上升"。这些因果知识不在本体中——它们需要通过物理仿真或经验模型来获取。但反事实推理的框架可以承载它们：将"V-12 阈值改为 250°C"作为反事实假设，调用外部仿真引擎获取因果链，将结果投影回本体的可能世界。

### 2.3 应急预案推演

系统当前状态：数据中心正常供电。应急指挥官提出反事实假设："假设 A 区两路市电同时中断，备用柴油发电机 B 启动失败，哪些设备的断电容忍窗口会在 30 秒内被突破？"

这种推理的特征是：反事实假设涉及的不仅是单个属性的改变（"V-12 阈值降低了"），而是一组相互关联的事实的联合改变（市电中断 + 备用发电机故障）。而且，推理的结果是**紧急的**——系统必须在几秒内给出答案，而不是用几小时做一次完整的全局仿真。

### 2.4 合规审计的事后追溯

审计员提问："在上周三的维护窗口期间，如果 V-12 没有被手动旁路，而是按照默认的自动关闭逻辑运行，当时的工作流是否会被中断？"

这种推理的特征是：反事实假设指向的是一个**过去**的时间点（"如果上周三没有旁路 V-12"），而不是当前或未来。推理需要将系统回滚到那个时间点上的本体快照，然后注入反事实假设，再运行推理规则。

这四种场景覆盖了反事实推理在工业系统中的主要需求范围。它们共同指向一种工程能力：在保留当前本体的推理规则的前提下，临时改变当前本体的部分事实，观察推理规则在这些改变后的事实上会产生什么结论。


## 3 方案一：世界分叉——以快照为分叉点，在分叉世界中运行全部推理规则

### 3.1 核心思想

世界分叉方案的基本操作是：取当前本体的一次快照，将反事实假设作为对快照的修改注入，在修改后的快照上运行全部可判定的推理规则，得到的结果就是反事实可能世界。

用哲学家熟悉的语言来说：**反事实假设定义了一个可能世界，现实世界的全部推理规则在这个可能世界中同样有效，推理规则在这个可能世界上的闭包就是该可能世界的全部事实。**

用工程师熟悉的语言来说：**Git 的分支操作 + 分支内的 CI 管道。** 取当前 main 分支的一次 commit 作为基线，创建反事实分支，在分支内修改若干文件（注入反事实假设），在分支内运行全部测试（推理规则），测试通过后的分支状态即为反事实结果。

### 3.2 操作流程

1. **基线快照**：取当前本体的最新稳定快照。
2. **分叉**：创建一个独立的、与基线共享除反事实修改外的全部事实的推理空间。
3. **注入反事实假设**：在分叉世界中将反事实假设作为事实修改写入。反事实假设可以是一个或多个属性的值改变、一个或多个实体的存在性改变、一个或多个关系的建立或移除。
4. **推理闭包计算**：在分叉世界中运行全部 DL 推理规则——包括 TBox 公理（概念包含、角色层次）和 ABox 断言的一致性检查。推理引擎不知道它在反事实世界中运行——它只是在给定的事实上运行，检查一致性、计算可推导的结论。
5. **结论提取**：将分叉世界中的可推导结论与基线世界对比，标记差异。

### 3.3 关键设计决策

**事实注入的范围。** 一个反事实假设在修改一个事实时，可能产生与其余事实的不一致性。"V-12 当前温度 500°C"且"V-12 当前已关闭"这两个事实如果同时存在，是否触发不一致？分叉方案的首要设计决策是：推理引擎在检测到不一致时应当如何反应——拒绝反事实假设（这意味着这个假设在逻辑上不可能）、标记不一致并报告来源（这意味着让工程人员判断这是不是一个真实的风险），还是自动修正假设以恢复一致性（自动推导出"如果 V-12 到了 500°C 但没关，那么关闭机制一定也失效了"）？本文的立场是：工程系统中保持一致性的优先级高于反事实推演的完整性——不一致的假设不应被自动修正，而应被标记为"在给定推理规则下不可实现"并返回给操作者。

**推理规则的完整性。** 分叉方案的可靠性完全取决于基线的推理规则是否完整。如果基线的公理没有覆盖 V-12 与 V-13 之间的流量再分配因果关系，那么即便是全局分叉仿真也无法推导出"V-12 阈值降低导致 V-13 负载上升"。这意味着：分叉方案的全局性是一个必要的优势——它不会遗漏任何已编码的推理规则——但它不是充分的：那些尚未被编码的因果知识，分叉方案同样无法自动补全。

**多个反事实假设的并发。** 同一个基线可以分叉出多个反事实世界，各世界独立运行推理。这在应急预案推演中特别重要——应急指挥官需要同时看到"如果启动备用发电机"和"如果启动电池组"两种方案的结果，而不是先看一个再看另一个。

### 3.4 与 Snapshot 的关系

《本体论的核心是什么》已经论证了 Snapshot 机制的工程定位——版本化管理本体变更。世界分叉方案将 Snapshot 的能力从"追溯"扩展到"前瞻"。$\mathrm{P}$（过去时）指向 Snapshot 的追溯功能：可以从当前状态回溯到任何一个历史快照。"如果上周三没有旁路 V-12"——这意味着先将本体回滚到上周三的快照，然后注入"V-12 未被旁路"的假设，再执行推理闭包。$\mathrm{F}$（将来时）指向世界分叉的前瞻功能：可以在当前本体的基础上创建多个可能的未来分支。世界分叉方案因此为第二篇中"方向明确但完整论证需另文展开"的时态算子消解问题提供了一个具体的工程机制。


## 4 方案二：变换函数——将反事实作为对现实事实的增量映射

### 4.1 核心思想

世界分叉方案的优势在于全局性——它不会遗漏任何推理规则——但它的代价是代价高昂。当反事实假设只涉及一个属性的改变，而整个本体包含数万个事实和数千条推理规则时，在分叉世界中运行全部推理规则的闭包计算是不必要的。变换函数方案提供了一种更低代价的替代：将反事实假设视为一个作用于现实世界特定事实上的变换函数 $f: (F, H) \mapsto F'$，其中 $F$ 是受影响的现实事实，$H$ 是反事实假设，$F'$ 是变换后的事实。推理不是在全部分叉世界上运行——只在受影响的局部范围内运行。

用哲学家熟悉的语言来说：**不是先定义一个完整的可能世界再在其中推理，而是先从反事实假设出发定义一个对现实世界的变换，然后观察这个变换沿着推理规则的传递链传播到哪里、在何处被吸收或阻断。**

### 4.2 操作流程

1. **假设解析**：将反事实假设解析为对本体中特定元素的操作——修改某个属性（Property）的值、移除某个链接（Link）、插入一个新的可能实体。
2. **影响域初筛**：根据反事实假设的直接作用对象，确定推理规则的初始受影响集。改变 V-12 的温度阈值，直接影响的是所有引用了 V-12 温度阈值的推理规则。其他不受影响的规则不触发重新推理。
3. **增量传递链计算**：从初始受影响的事实出发，沿推理规则的前向链逐层传递。如果某一条规则的输入未因前序传递而改变，则该规则隐含的结论不改变——传递在该规则处终止。
4. **可能世界投影**：将增量传递链上所有被改变的结论和不受影响的结论合并，投影为一个局部可能世界——它不是对全部事实的完整重写，而是对基线事实的差异描述。$\text{World}_{H} = \text{World}_{0} \cup \Delta_{H}$，其中 $\Delta_{H}$ 是递增传递链上所有事实变化的有向集合。

### 4.3 关键设计决策

**变换函数的可逆性。** "将 V-12 的触发阈值从 300°C 改为 250°C"——这个变换显然是不可逆的——在现实世界中，一旦某个设备按某个阈值运行了一段时间，该阈值就不能被逆转。变换函数方案不要求反事实假设在现实世界中是可逆的——只要求它在逻辑上是一致的。它的输出是"如果当初如此，现在会如何"——一个不声称自己可实现的推理结果。

**传递链的终止条件。** 传递链在遇到以下情况时终止：推理规则的所有输入均未变化（该规则隐含的结论不变）；推理规则产生了一个逻辑矛盾（该假设导致不一致，标记并终止该条链）；传递链进入了未被本体覆盖的领域（推理规则无法应用，终止并标记为"影响范围超出已知推理边界"）。最后一种终止状态是变换函数方案的关键设计：它告诉操作者"根据已知的推理规则，到这里为止我们能推出来；再往前，需要你告诉我更多规则。"

**增量传递对 DL 推理复杂度的影响。** 在 DL 的标准推理问题中，一致性检查的成本与事实断言集的大小正相关。分叉方案需要对整个断言集做一致性检查——成本与本体规模成正比。变换函数方案只对受影响的断言子集做一致性检查——成本与影响范围成正比。对于大多数局部反事实推理（改一个阈值、加一个设备、移除一个链接），影响范围远小于全局本体，变换函数方案因此比世界分叉方案更轻量。

### 4.4 典型适用场景

变换函数方案最适合设计变更评估（场景 2.2）——"如果只改 V-12 的阈值，其余设备会不会受影响"。在这种场景中，反事实假设只涉及一个属性的改变，传递链在几步之内即可终止（或标记为未知）。对于应急预案推演（场景 2.3），变换函数方案则不太合适——当反事实假设涉及一簇相互关联的事实改变时，影响范围可能很大，传递链的分支数量急剧增加，分叉方案的"全量闭包"反而更简单可靠。

### 4.5 变换函数与可能世界在逻辑性质上的差异

这里值得指出一个重要的区分。IL 中的可能世界语义，是把整个模型结构的副本（一个世界就是一个模型）作为一个语义对象来操作的。变换函数方案不是这个意义上的"可能世界"——它不复制整个模型。它是一个作用于现实模型上的推导函数，它的输出不是一个完整的模型副本，而是一个对现实模型的标定差异。它在操作上的效率来自这个差异——它不需要复制整个模型来推演一个属性的改变。

但这同时也意味着：变换函数方案不能处理那些"因为有某个事实的存在，所以某个推理规则被激活"的情况——因为变换函数方案只追踪被改变的事实，它不追踪"现有的某个事实因为在反事实假设下变得相关"这种反方向的依赖。当推理规则的触发条件与其结果之间存在非局部的依赖时，变换函数方案的低估风险就会出现。


## 5 两种方案的适用边界

世界分叉方案和变换函数方案不是两个优劣选项，而是两种为不同工程场景设计的反事实推理工具。它们的适用边界如下：

| 维度 | 世界分叉方案 | 变换函数方案 |
|---|---|---|
| 原理 | 分叉快照，在完整副本上运行全部推理 | 增量传递链，计算受影响的差异子集 |
| 可靠性 | 高——不遗漏任何已编码的推理规则 | 中——可能遗漏非局部的因果关系 |
| 计算成本 | 与本体规模成正比 | 与影响范围成正比 |
| 适用假设 | 全局性假设（多事实联合改变） | 局部性假设（单事实或局部簇的改变） |
| 典型场景 | 应急预案推演、合规审计追溯 | 设计变更评估、传感器失效仿真 |
| 与 Snapshot 关系 | $\mathrm{F}$（将来时）和 $\mathrm{P}$（过去时）均可 | 主要指向 $\mathrm{F}$——增量前景推演 |
| 逻辑要求 | 需要一致性的全量检查 | 只需受影响子集的局部检查 |

选择逻辑是：如果反事实假设涉及一组相互关联的非局部事实改变——"A 区停电 + 发电机失效 + B 区负载转移"——用世界分叉方案。如果反事实假设只涉及一个局部属性的改变——"V-12 阈值从 300 改为 250"——用变换函数方案。如果无法确定反事实假设的影响范围——先用变换函数方案做初筛，如果传递链触发了"已知推理边界外"的标记，再升级为世界分叉方案。


## 6 反事实推理在论文序列中的位置

### 6.1 与模态可能 $\lozenge$ 的关系

《本体论的核心是什么》将 $\lozenge$ 的消解标注为"尚未闭合"——"可能"在工程系统中分裂为容许性、不确定性与潜在性三种语义，三者无法被统一机制统摄。反事实推理为这个问题提供了一个重要的推进：它处理的是三种语义之外的第四种"可能"——**假设性可能**。它与容许性、不确定性、潜在性的关键区别在于：它的来源不是系统内部状态的自然分化，而是**人的有意识的假设注入**。工程人员主动说"假设 X 成立，推理一下会怎样"——这种"可能"不是系统观察到的（潜在性），不是系统正在实验中验证的（不确定性），不是系统允许但不要求的（容许性），而是**被人为构造出来的**。假设性可能为 $\lozenge$ 的四种语义（容许、不确定、潜在、假设）提供了完整的语义图景——而反事实推理本身就是处理假设性可能的工程机制。

### 6.2 与时态算子 $\mathrm{F}$ 和 $\mathrm{P}$ 的关系

《本体论的核心是什么》将时态算子的消解方向标注为"方向明确但完整论证需另文展开"。世界分叉方案对此提供了具体的工程承载：$\mathrm{P}$（过去反事实）通过回滚到历史快照再注入假设完成——世界分叉的基线是历史上的某个 Snapshot；$\mathrm{F}$（将来反事实）通过在当前快照上分叉多个可能的未来路径完成——世界分叉的基线是当前的最新快照。这为时态消解论证提供了完整的工程机制描述。

### 6.3 与符号圈的关系

《本体论下的自演进自学习组织体如何成为可能》指出模态可能和时态算子在语义层难以形成闭环，"语用检验遂成为其主要判据"。反事实推理为这一判断提供了进一步的证据：无论是世界分叉方案还是变换函数方案，它们对一致性检查的依赖（推理规则的闭包计算）都属于语法检验——"系统能否在给定规则下从假设中推导出无矛盾的结果"。但在推导出结果之后，结果的工程意义的判断——"V-12 阈值降低导致 V-13 负载上升 12%，这个代价是否可接受"——不在语法检验的范围之内，也不在语义检验（是否映射到稳定的 sign-object 关系）的范围之内。它属于语用检验——"在当前的业务场景和运维预算下，这个结果是否可接受"。反事实推理因此是从语法-语义检验进入语用检验的最典型的桥梁。而后续关于 LLM 的论证——LLM 在语用判断上的不可替代性——在这里获得了最自然的应用场景：LLM 可以辅助操作者判断反事实推演结果的业务可接受性。

### 6.4 与 IL 降维的闭合

现在可以为《蒙太格的遗产》中 IL → FOL 降维的"工程牺牲"提供一个完整的补遗。降维表格的第一行写着：放弃二阶量化和内涵语境，获得半可判定推理——Montague 第三重承诺（可能世界语义）的工程牺牲。

本文的论证为这一行添加了一个脚注：**可能世界语义的工程功能损失，不是由更精细的逻辑来填补的，而是由工程系统外部的推理机制——世界分叉和变换函数——来填补的。** 这反过来印证了《蒙太格的遗产》的核心论点：逻辑学不能独立完成自然语言工程化的全部任务——在逻辑不可判定性所划定的硬边界之外，需要别的东西来接续。本文提供了那个接续物在"可能世界/反事实"这一特定领域的具体形态。


### 6.5 溯因推理：反事实推理的反向

本文的反事实推理——世界分叉和变换函数——处理的是"如果假设X成立，结果会是什么"：从假设到结论的正向推演。但在工业场景（尤其是故障诊断）中，存在一个同样重要的反向需求：系统观察到一个异常状态，然后反向推导——"哪些因果链可能导致这个异常？"这不是规则的执行，而是规则的**逆推**。给定一条规则"如果 V-12 超温，则关闭 V-12"，正向推理（演绎）是"超温 → 关闭"。但故障诊断面对的是反向："关闭发生了，但不是因为超温——那到底是因为什么？"

《本体论下的自演进自学习组织体如何成为可能》的 Action 设计——`triggers` 和 `observes`——为因果链提供了结构基础，但并未提供溯因的算法逻辑。这是有意为之：溯因推理本质上是一种假设生成（hypothesis generation），而不是一种逻辑推导（logical deduction）。它需要系统从无限多的可能原因中筛选出一个有限的可疑集合——而这个筛选过程本质上不是形式逻辑的，而是基于**经验权重**的。

"V-12 上次出故障是因为震动超标，这次震动传感器数据也是异常的——优先检查震动。"这个推理不是"从前提推出结论"，而是"从结论出发，在可能的前提空间中做加权搜索"。在这个意义上，溯因推理是反事实推理的逆向版本：反事实推理问的是"如果前提改变，结论如何改变"，溯因推理问的是"如果结论异常，哪个前提的改变最可能导致了它"。两者共享同一个因果结构——Action 的 triggers 和 observes——但遍历方向相反。第六篇解决了正向遍历的工程机制，溯因（反向遍历）的工程机制需要一种反事实推理本身不提供的能力——在多个可能前提之间基于经验数据做加权排序——这是后续论文中神经网络（LLM）需要承担的任务。


## 7 结论：可能世界从未真正离开

Montague 的可能世界语义被 IL → FOL 的降维路线拿掉了。但"如果……会怎样"这个问题没有随之消失。它只是换了一种形式继续存在——从逻辑系统的内部装置变成了工程系统的外部工具。

世界分叉方案和变换函数方案是对这一转型的具体回答。它们不是在 DL 推理引擎里重建 IL 的可能世界——那个方向已经被可判定性边界彻底关闭。它们在推理引擎外部构建了两种反事实推理机制，分别处理全局假设和局部假设。它们共同证明了一件事：可能世界从未真正离开工程本体论。它只是从"逻辑内部"转移到了"系统边界"——从 Montague 的内涵逻辑中走出来，走到了工业系统的快照机制、推理闭包计算和增量传递链之中。

这一转移不是退步。IL 的可能世界是不可判定的——它们可以容纳任何强度的假设，但无法保证推理在有限时间内终止。工程本体论的反事实推理是可判定的——它们被快照机制的版本边界和变换函数的传递链终止条件严格约束，代价是反事实假设的表达力被限制在本体可编码的事实范围内。从"不可判定的可能性"到"可判定的反事实推理"——这恰恰是降维路线最深刻的工程含义：不是放弃一种能力，而是为一种不可判定的人类认知能力找到了可判定的工程替代。

---

作者：顾昊
