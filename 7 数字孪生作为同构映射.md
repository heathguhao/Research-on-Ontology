# Digital Twin as Isomorphic Mapping: Ontology, Data, and Action

**数字孪生作为同构映射：本体、数据与动作**

---
作者：顾昊



\medskip

\noindent\textbf{Keywords:} digital twin $\cdot$ isomorphism $\cdot$ bijection $\cdot$ homomorphism $\cdot$ ontology delivery $\cdot$ data governance $\cdot$ action design $\cdot$ compositionality $\cdot$ engineering ontology

\medskip

\noindent\textbf{AMS Subject Classification:} 68T30 (Knowledge representation) $\cdot$ 68U35 (Information systems) $\cdot$ 00A71 (Theory of mathematical modeling)

---


## 摘要

前六篇论文已经建立了工程本体论的完整理论体系。现在需要回答一个实践性问题：这套理论在交付给客户时，最终呈现为哪些可用的产物？本文以"数字孪生"为组织性概念，论证工程本体论所追求的数字孪生不是可视化或仿真，而是数字世界对**真实业务**的同构映射——双射的本体端符号，对应的不是客户现有的数据或表单，而是客户的真实业务现实。然而绝大多数客户的真实业务并不存在于他们的数据系统中——客户的数据是脏的、失真的、跨系统之间各自为政的。因此，映射规则不仅仅是"把数据挂到本体上"——它首先是把客户混乱的数据还原为反映真实业务的数据，再将真实业务数据映射为本体的双射实例。这意味着我们的交付物中隐含着一个数据中台层：客户脏乱的跨系统数据 → 统一的真实业务数据 → 基于真实业务的抽象本体。基于此，本文给出三种面向客户的交付物：一套基于真实业务逻辑建立的抽象本体，一套包含数据中台与双射映射在内的映射规则体系，以及一组从本体结构和真实数据中生长出来的 Action。


## 1 双射的对象：不是数据，是真实业务

数字孪生的严格定义是数字世界对现实世界的同构映射。双射要求本体中的每一个符号与现实中的一个对象一一对应——但这个"现实"指的究竟是什么？

一个自然的回答是：现实就是客户系统里的数据。客户有 SCADA，有 MES，有 ERP，有巡检记录——这些数据描述了客户的业务现实。双射就是把这些数据中的实体和属性，一一映射到本体中。

**这个回答在工程上是错的。** 它错误地预设了客户的数据已经忠实地反映了客户的真实业务。而工业现场的真实图景是：

- 同一台阀门在 SCADA 里叫 `V-12`，在 MES 里叫 `VALVE_B3_012`，在维护数据库里叫 `EQ-2021-0457`——但没有一个系统知道这三者是指同一台设备。
- SCADA 的温度传感器数据是准确的，但 MES 中同一台设备的温度字段是一个月前手工录入的。
- ERP 里的设备台账列了 200 台泵，现场实际只有 187 台在运行，另外 13 台已经拆除但台账从未更新。
- 巡检记录中写的是"V-12 温度正常"，但同一天 SCADA 的历史数据显示 V-12 的温度传感器因故障返回了固定值 25°C。

这些不是"数据质量问题"，不是"再清洗一下就好了"。这是**客户的数据系统没有承载客户的真实业务**。客户的数据系统承载的是各个业务部门各自维护的、彼此不一致的、随时间漂移的局部记录。真实业务——哪台设备正在运行、哪条管道连着哪台阀门、哪个班组在什么时间做了什么操作——并不存在于任何一个单一的系统中。它分布在多个系统的缝隙里、隐含在经验的判断里、存在于一线操作员的头脑里。

因此，双射不能建立在"本体 ↔ 客户现有数据"上。如果本体的双射对象是脏数据，那么本体内部的推理结果——无论逻辑上多么自洽——对应的是虚假的现实，而不是真实的业务。**双射的正确定义是：本体符号 ↔ 真实业务现实。** 双射要求本体中的每一个 ObjectType 实例对应现实中的一台真实设备、一条真实管道、一个真实班组，而不是对应某个数据库中的某条记录。

这意味着映射规则要做的事，比"把数据挂到本体上"复杂得多。它首先必须**从客户混乱的数据中还原出真实业务**——不是修复数据，而是重建现实。以下三节将这个逻辑顺序展开为具体的工程交付物。


## 2 交付物一：抽象本体

抽象本体是客户的真实业务在符号系统中的完整表达。它回答的问题是：**在客户的真实业务中，有什么？**

### 2.1 六个核心分类

- **ObjectType**：客户业务中的实体类型。物理实体（阀门、管道、泵组）、组织实体（部门、班组、维护单元）、流程实体（工单、巡检任务、审计记录）。每一种 ObjectType 对应真实业务中一类可以被独立识别和追踪的对象——不是数据库里的一条记录，而是车间里的那台设备。
- **LinkType**：实体之间的结构关系和因果链路。Link 的方向、源类型和目标类型决定了系统的拓扑骨架。一条管道连接两个阀门、一个班组负责一组设备、一条告警规则指向一个传感器——这些都是真实业务中的关系，在数据系统中可能完全不存在对应的字段。
- **PropertyType**：实体可以被赋予的状态描述及其数据约束。Property 必须声明数据类型、取值范围、单位和数据来源。Property 是本体与数据的接口——它是唯一携带值的东西。
- **ActionType**：系统可以在实体上执行的变更操作。Action 声明了前置条件、参数、副作用。Action 是本体与外部世界的接口——它是唯一能改变状态的东西。
- **InterfaceType**：跨类型的组合约束。一个 ObjectType 必须包含哪些 Property、必须参与哪些 Link。
- **SharedPropertyType**：可跨 ObjectType 复用的属性模板。

### 2.2 三个质量标准

抽象本体的质量由三项工程指标评判：

- **完整性**：客户的真实业务对象是否都被覆盖？关键操作流程是否能被 ObjectType-LinkType-ActionType 的网络完整表达？
- **一致性**：同一个概念在本体中是否始终意味着同一件事？Link 的 source 和 target 是否指向已定义的类型？Action 的参数是否与目标 Property 相容？
- **可演进性**：本体是否能在不拆掉现有结构的前提下增加新的类型？

### 2.3 交付格式

抽象本体的最终交付格式是 proto 定义文件。它是一份可以直接在工程中使用的类型定义，而不是一份文档。


## 3 交付物二：映射规则（含数据中台）

抽象本体规定了"真实业务中有什么"。但真实业务的图景并不直接存在于客户的任何一个数据系统中。映射规则是整个交付体系中工作量最大的一块——它要完成的不是"一步映射"，而是**两阶段重建**：

> 客户脏乱的、跨系统的原始数据 → 统一的、反映真实业务的干净数据 → 基于真实业务数据的本体实例

第一阶段是数据中台的任务：把多个系统中各自为政的、彼此矛盾的、随时间漂移的原始数据，聚合并还原为一套统一的、描述真实业务的数据视图。第二阶段才是双射映射：将这套统一视图中的每一个实体、每一条属性、每一段关系，一一对应地实例化为本体中的符号。

逻辑上，这个顺序是严格的——你必须先知道真实业务长什么样，才能把本体建立在它上面。但在工程实施中，两个阶段是交替推进的：发现数据中的矛盾会促使你修正对真实业务的理解，修正后的业务理解会要求你重新审视数据。本体设计、数据中台建设、双射映射——三者在一个迭代的反馈环中相互校正。

### 3.1 第一阶段：数据中台——从混乱数据到真实业务

客户的数据通常存在于三到五套互不通信的系统中，每一套都维护着对同一事实的局部记录。数据中台的任务不是"同步这些系统的数据"，而是**以真实业务为基准，重新组织这些数据**。

具体包括三个子任务：

**实体统一**：识别不同系统中哪些标识符指向真实业务中的同一个实体。SCADA 的 `V-12`、MES 的 `VALVE_B3_012`、维护系统的 `EQ-2021-0457`——数据中台需要建立一张跨系统的实体对应表，声明这三者是同一台阀门的三种称谓，并在数据中台层为其分配唯一业务标识符。这个任务不能仅靠字符串匹配或规则自动化——它需要结合设备位号命名规范、厂商序列号、安装位置等多维线索进行人工判定。

**属性冲突消解**：当多套系统对同一实体的同一属性给出了不同的值，哪一个才是真实的？SCADA 的温度数据是传感器直读，MES 的温度数据可能是手工录入的滞后值。数据中台需要为每一条属性建立"数据源可信度排序"——不是简单地选一个"最准"的源，而是建立"在什么条件下信任哪个源"的规则：正常运行时信任 SCADA，SCADA 标记传感器故障时切换至 MES 人工录入值并标记数据可信度为"降级"。这不是数据格式的转换，而是**业务语义的判断**。

**缺失实体的发现**：ERP 台账列的 200 台泵中，13 台已经拆除但台账从未更新。数据中台不能假设"数据库里有的就是现实中有的"。它需要交叉比对：SCADA 是否有该设备的数据流、巡检记录中是否出现过该设备、最近一次维修工单是什么时候。当多个系统一致显示某个实体不存在时，数据中台将之标记为"台账冗余"而非静默复制到统一视图中。反过来，如果一线操作员口头确认存在某台设备但在所有系统中都找不到记录，数据中台需要建立占位实体并标记"待数据源确认"。

数据中台的输出，是一套独立于原始系统的**真实业务数据视图**。它不与任何单一系统绑定——SCADA 可以替换、MES 可以升级，但真实业务数据视图不变。它的每个实体都有唯一标识，每项属性都有明确的数据源和可信度标记，每段关系都有跨系统的交叉验证。

### 3.2 第二阶段：双射映射——从真实业务数据到本体实例

当真实业务数据视图建立之后，双射映射才是直接的操作：将视图中的实体、属性和关系，一一对应地实例化到抽象本体的符号系统中。

**满射方向**（本体中的每个符号都对应真实业务中的一个东西）：抽象本体中定义了 ObjectType `Valve`——真实业务数据视图中必须有至少一个阀门实体，其所有必填 Property 都有值，所有 InterfaceType 要求的 Link 都有对应的关系实例。如果本体中定义了 Property `temperature`，真实业务数据视图中必须有这个属性的数据源和可信度标记。

**单射方向**（真实业务中的每个东西在本体中只有一个符号）：数据中台层已经完成了跨系统实体的统一，双射映射在此基础上的工作是将统一后的实体标识符映射为本体中的唯一 RID。`UNIFIED_DEV_0042` → `v-12`。一条映射，对应关系明确。如果真实业务数据视图中新增了一台设备，它在本体中获得一个新的 RID，不与任何已有 RID 重复。

### 3.3 双射的持续性

双射不是一次性工程。真实业务数据视图会随着新数据源的接入和旧数据源的退役而更新，本体会随着业务理解的变化而演进。双射的持续性意味着：每一条新进入数据中台的原始数据，在被纳入真实业务数据视图之前，必须回答——它对应的是现实中哪个实体（单射校验），它声称的实体在真实业务中是否存在（满射校验）。违反双射的数据不应静默进入视图——应当被标记、报告、并触发人工判断：是数据中台的实体统一规则需要修正，还是现实中确实发生了变化需要更新本体。


## 4 交付物三：动作（Action）

抽象本体规定了真实业务的结构，映射规则还原了真实业务的图景并建立了双射，但系统还没有"功能"。动作是第三件交付物——它回答的问题是：**这个系统能做什么？**

### 4.1 每个 Action 就是一个功能

在客户的视角下，Action 就是他们购买这个系统后可以实际使用的功能。"超温自动关阀"是一个 Action，"生成月度巡检工单"是一个 Action，"追溯事故因果链"是一个 Action。

在工程上，每个 Action 定义了：

- **触发条件**（`trigger`）：在什么状态下执行——可以是指定时间、事件到达、某个 Property 越过阈值、或是另一个 Action 的副作用。
- **操作对象**（`target`）：作用于哪些实体。对象必须是在抽象本体中已定义的 ObjectType 的实例。
- **操作内容**（`operation`）：做什么——修改 Property 值、创建或删除 Link 实例、调用外部 API。
- **副作用**（`side_effect`）：执行后会改变系统的哪些部分。这是同态条件的工程体现。
- **安全级别**（`safety`）：谁能触发、需要什么权限、是否有回滚机制。

### 4.2 Action 从本体与真实数据中生长出来

Action 是在本体和映射规则就位之后生长出来的。一条"超温关阀"的 Action，依赖于：本体中定义了 Valve 和 `temperature`；数据中台为每一个阀门的温度传感器建立了可靠的数据源和可信度排序；双射映射将每一个统一实体实例化到了本体的 RID。在此基础上，Action 的触发条件写成 `temperature > 320°C`，操作写成 `SET valve_status = CLOSED`。如果本体中没有定义 `temperature`，Action 写不出来。如果数据中台没有为温度传感器建立数据源可信度排序，Action 不知道在传感器故障时应当信任哪个备选源。

### 4.3 Action 的类型

- **规则 Action**：由系统自动触发，逻辑必须可判定。
- **交互 Action**：由人触发，需携带角色-权限约束和审计日志。
- **事件 Action**：由外部事件触发，是同态映射的实时执行点。


## 5 三层交付物的关系

三件交付物之间有明晰的依赖关系：结构先于数据，数据先于功能。没有本体，映射没有靶子——你不知道数据要往哪挂。没有数据中台和映射，动作没有可信的数据源——你不知道要操作的对象当前处于什么状态。没有动作，本体和映射就是一个只能看不能动的档案库。

但工程实施的路径取决于客户的起手状态。

如果客户的数据治理已经成熟——跨系统的实体标识符统一、多源数据的一致性有保障、台账与现场实际一致——那么数据中台的工作量极轻，可以直接从客户数据中提取真实业务图景，在此基础上建立本体。这种情况下的顺序就是"数据→本体→动作"。

如果客户的数据是脏乱的、失真的、彼此矛盾的——这是更常见的情况——无法直接从数据中还原真实业务。此时的做法是：先通过业务访谈和现场调研建立本体的骨架（对真实业务的初步假设），再以这个骨架为指导去建设数据中台（知道要还原哪些实体、哪些属性、哪些关系），在数据中台建设过程中发现新的业务事实，回头修正本体，如此反复迭代。本体和数据中台在相互校验中收敛。

两种情况没有本质区别——最终都是本体与真实业务实现双射。区别只在于是从数据出发逼近本体，还是从本体假设出发校验数据。选择哪条路径，不取决于理论，取决于客户数据今天的质量。


## 6 交付的边界诚实性

工程本体论的交付原则中有一项核心的**边界诚实性**：系统只承诺它已经编码的东西。

同构映射所保持的"结构"，不是现实业务的全部，而是已经被编码为本体推理规则的那部分结构。Action 的触发条件覆盖的范围，不是在现实中可能发生的一切，而是在本体中已经声明的那部分条件。数据中台还原的真实业务，不是"对车间里发生的一切的全知全能复刻"，而是"在已接入的数据源和已建立的实体统一规则覆盖范围内的最佳重建"。

这意味着：

- 本体的边界：哪些 ObjectType、LinkType 被包含，之外的实体和关系未被建模——明确标注。
- 数据中台的边界：哪些实体已建立跨系统统一标识，哪些还存在标识冲突待人工判定——明确标注。
- 双射的边界：哪些 Property 有数据源且可信度为"正常"，哪些处于"降级"状态——明确标注。
- Action 的边界：每个 Action 的作用域和失效条件——明确标注。

数字孪生不承诺全知全能。它承诺的是：在已交付的范围内，符号跟现实一一对应，规则的推导结果跟真实业务的因果一致。边界之外，系统诚实地回答：**我不知道。**


## 7 结论：三件东西，一份承诺

工程本体论给客户的不是一个"软件系统"，而是一个数字世界对真实业务的同构映射。这个映射拆解为三件可逐项验收的交付物：

1. **抽象本体**——基于客户的真实业务逻辑建立，规定了系统中有什么、它们之间是什么关系。
2. **映射规则（含数据中台）**——将客户脏乱的、跨系统的原始数据，先还原为统一的真实业务数据视图，再将真实业务中的每一个实体、属性和关系，一一对应地实例化到本体中。没有这一层，本体只是一个概念模型；没有数据中台，映射没有可信的源。
3. **Action**——在结构和数据都就位之后，让系统能动起来、能做事、能在规则范围内替代现实做推演。

三件交付物合在一起，构成数字孪生——不是可视化或仿真，而是数学承诺：在已编码的推理规则覆盖范围内，数字世界的推演结果对应于真实世界的事实际遇。这个承诺建立在双射和同态的同时成立之上——双射保证本体对标的是真实业务而非脏数据，同态保证规则在数字世界中等效运行。

前六篇论文论证了这套理论的自洽。本文论证了这套理论可以被翻译为可交付、可验收的工程清单。而数据中台——这个在本文中首次被明确提出的工程组件——是将理论转化为实践的关键桥梁：它把客户手中那些不能信任的数据，转化为本体可以信赖的真实业务基础。

---

作者：顾昊
